# Listener Product Requirements

This registry records durable owner decisions that span Listener firmware and
Listener Type. New behavior must preserve earlier accepted entries unless the
owner explicitly replaces them.

| ID | Owner date | Requirement | Machine evidence |
| --- | --- | --- | --- |
| LST-OTA-001 | 2026-07-29 | Restore the EC11 knob-ring and board-edge OTA progress/chase renderers exactly to `v1.0.2`. The single status light must not breathe: keep it steady cyan and map transferred-byte progress monotonically from 18% to 52%, matching the `v1.0.2` brightness range without its 1300 ms triangle pulse. Keep the OTA BLE surface steady, do not add write-tick flashes, and do not add a pre-reboot success flash. This entry replaces all earlier same-day full-`v1.0.2` and full-brightness status interpretations. | Static contract comparing EC11/edge renderer paths with `v1.0.2`, asserting a monotonic 18%-52% status progress mapping with no pulse/write tick, plus a real OTA visual acceptance and serial trace. |
| LST-OTA-002 | 2026-07-29 | A successful OTA causes exactly one intentional device reboot into the new image. A second real reboot is a defect and must be fixed at its reset/rollback source, not hidden by suppressing a normal boot cue. Every real boot may show the normal boot cue once. | Reset-reason/boot-partition serial trace across a real OTA; exactly one post-FINISH boot banner and no rollback/reboot event for a healthy image. |
| LST-OTA-003 | 2026-07-29 | After the OTA reboot, the existing Windows bond and Listener Type session recover automatically without manual unpair/re-pair or restarting Type. Merely detecting the device after a long delay is not accepted: the persisted-device warm path retains the accepted start-to-notify-ready target of at most 1000 ms once Windows exposes the device. A verified controlled OTA handoff reuses the bonded Windows CCCD subscription that the OS restores during reconnect instead of issuing a redundant descriptor write against a pre-reboot handle. Diagnose the actual post-OTA delay from installed-Type logs without reducing OTA throughput. | Installed-Type OTA log with timestamped advertisement/device exposure, service confirmation, reused CCCD, stable `TYPE:READY`, and a subsequent embedded audio session. |
| LST-OTA-004 | 2026-07-29 | Preserve the 1.0.3 OTA throughput improvement while fixing reliability. Keep dual DATA/DATA_B write-without-response transfer, the 32-entry device worker pipeline, and the 400-chunk active-link host window. Do not reduce the hot-path window to address sync/reconnect failures. | Static constants plus real OTA `bulk_kb_s`; compare against the current 1.0.3 active-link baseline and flag a material throughput regression. |
| LST-OTA-005 | 2026-07-29 | When Listener Type restores the audio notification path after an OTA reboot, show exactly one quiet grey completion capsule with the `v1.0.2` text `Listener 音频已恢复`. The OTA success capsule must not be suppressed merely because automatic recovery required repeated attempts, and it must not leak into unrelated later reconnects. | Focused coordinator test proving one capsule for the matching OTA recovery generation, no duplicate emission, and no stale emission on unrelated recovery. |
| LST-OTA-006 | 2026-07-29 | Listener Type must abort an OTA transfer when an observed BLE transfer error cannot recover for 10 continuous seconds, or when successful SYNC/status cycles confirm that the device offset has stopped advancing for 10 seconds. The watchdog is not a total-transfer deadline and must not count an opaque, still-running device flash/SYNC operation as inactivity: the 400-chunk window can legitimately spend more than 10 seconds draining flash. Successful device-confirmed offset progress resets the no-progress clock; a recovered transport operation clears the error streak. On timeout, Type reports an explicit interrupted/stalled upgrade error, sends only a bounded best-effort ABORT, releases the OTA gate, and restores the background audio connection. The watchdog must not reduce the 1.0.3 transfer window or throughput. This clarification replaces the earlier same-day interpretation that timed every interval between confirmed offsets, which falsely aborted a healthy 400-chunk SYNC. | Host transfer tests with a transport that stalls after partial progress and one that continues making progress for longer than 10 seconds total; Type tests for a transient SYNC error that recovers and a continuous 10-second BLE error streak; installed-Type recovery log after an injected stall. |
| LST-OTA-007 | 2026-07-29 | Keep the accepted `v1.0.2` OTA renderers for the EC11 knob ring and board-edge LEDs unchanged. The single status LED remains a non-breathing, monotonic brightness progress indicator, but its minimum-to-maximum brightness difference must be clearly visible on the physical device; widen and quantize the progress brightness range without adding animation, write-tick flashes, or changing the EC11/edge behavior. This entry refines only the status-light brightness range in LST-OTA-001. | Static contract proving the EC11/edge OTA paths remain unchanged and the status LED uses a wider monotonic stepped range with no time-based pulse, followed by a real OTA visual acceptance. |
| LST-OTA-008 | 2026-07-29 | A failed, interrupted, or timed-out OTA must preserve the pre-existing Windows pairing and firmware bond. The immediate post-OTA recovery generation may retry the bonded GATT/notify path, but must not classify transient OTA disconnect or CCCD errors as stale pairing, invoke automatic Windows unpair, reset the firmware bond, open a PairAsync confirmation flow, or emit the successful-audio-restored capsule. Destructive pairing recovery remains available only from an independent later user/hardware recovery action, not from the OTA failure itself. | Focused Type tests proving every post-handoff failure restores through a generation-scoped non-destructive path, skips stale-pair cleanup, preserves the success-only capsule contract, plus an installed-Type injected-stall log showing the existing pairing survives and notify reconnects. |
| LST-OTA-009 | 2026-07-29 | An incomplete OTA transfer must never reboot the device. In particular, the 1.0.3 dual-lane/32-entry worker pipeline must not starve either watched idle task or run flash writes from a NimBLE GATT callback long enough to trigger the 5-second Task WDT. Reasserting the already-enabled OTA power blocker for each 500-byte write must also be idempotent: it must not wake and reevaluate the power-manager task for every packet. Keep the 400-chunk host window and accepted throughput; bound worker flash batches with a scheduler opportunity, make SYNC/status wait for the worker-owned flash pipeline instead of draining flash on the BLE callback task, and notify power management only when blocker/state actually changes. | Static ownership/scheduling contract, firmware build, reset-reason and partition trace, then at least three consecutive real OTAs with no Task WDT, each meeting the existing throughput gate and followed by notify-ready; the final run also includes an embedded audio session. |
| LST-OTA-010 | 2026-07-29 | OTA transfer and voice recording are mutually exclusive from the live `TYPE:OTA` handoff through the whole device-side OTA session. The handoff must establish the OTA preparation lease before canceling any hidden automatic wake candidate already using the audio stream. While either that preparation lease or the OTA power blocker is active, firmware must disable and reset voice-activation monitoring before processing queued VAD input and reject any new automatic recording start; speech or ambient noise must not open a recording session or make Type show a recording capsule. `TYPE:READY`/normal heartbeat recovery releases a failed handoff lease. This must not reduce the dual-lane/32-entry/400-chunk OTA throughput path. | Static guard proving the handoff cancels an existing busy audio session, both OTA phases block idle voice monitoring and queued VAD processing, firmware build, and real OTA serial evidence with no `voice_activation.auto_start`, recording-session start, BLE audio stream, or recording capsule event during transfer. |
| LST-OTA-011 | 2026-07-29 | OTA throughput must return to the demonstrated fast 1.0.3 level, approximately 50 KiB/s and never below 45 KiB/s sustained `bulk_kb_s` in the local installed-Type machine loop. Treat both 7.5 ms and the 15 ms interval selected by Windows `ThroughputOptimized` as valid active-transfer links (`itvl=6..12`, latency 0), with a matching 7.5-15 ms connection-event range (`min_ce_len=12`, `max_ce_len=24`). If the live link is already in that range, firmware must not issue connection-parameter updates; in particular it must not repeatedly fight Windows with a rejected 7.5 ms-only request during transfer. This corrects the same-day 7.5 ms-only interpretation after real status-571 evidence showed Windows retaining 15 ms. | Static connection-parameter contract plus installed-Type real OTA timing with `bulk_kb_s >= 45.0`, no repeated `BLE_ERR_CONN_PARMS`/status 571 loop, and the existing reliability/reconnect gates. |
| LST-OTA-012 | 2026-07-29 | OTA worker scheduling must use real FreeRTOS ticks. With the production 100 Hz tick, `pdMS_TO_TICKS(1)` is zero and must not be used as the worker/SYNC wait because a high-priority NimBLE callback can busy-yield while starving the lower-priority Flash worker. Drain at most the existing 32-entry queue per worker batch, then delay exactly one tick; the SYNC/status queue wait must also delay one tick so the worker can run. Keep Flash ownership on the worker and retain the no-WDT blocker fix. | Static tick/batch contract, build, and real OTA evidence showing the worker-owned path returns to `bulk_kb_s >= 45.0` with no Task WDT. |
| LST-OTA-013 | 2026-07-29 | Restore the demonstrated 1.0.3 SYNC/status fast path after real timing showed that waiting for the lower-priority worker raised `control_write_ms` from about 26 seconds to 67 seconds. The worker remains the normal DATA writer and is bounded to one 32-entry batch followed by one real tick, but a serialized SYNC/status callback may drain only the fixed queue tail while holding the existing OTA lock before reporting offset. This bounded tail assist replaces only the no-callback-Flash interpretation in LST-OTA-009/LST-OTA-012; the idempotent OTA power blocker, WDT protection, dual lanes, queue depth 32, and host window 400 remain mandatory. | Static bounded-tail ownership contract plus at least three consecutive real OTAs at `bulk_kb_s >= 45.0`, with no Task WDT and no offset recovery. |
| LST-OTA-014 | 2026-07-29 | Restore the original fast 1.0.3 worker behavior after the fixed one-tick delay following every full 32-entry batch measured only 18.9-25.8 KiB/s. The worker may continuously drain the fixed 32-entry queue while data remains, feeding its own watchdog after every flash write; SYNC/status retains the serialized fixed-tail assist. The captured WDT root cause was repeated wakeup of `power_manager_t` while reasserting an unchanged OTA blocker, so retain that idempotence fix instead of throttling the transfer. This entry replaces only the per-batch-delay requirements in LST-OTA-009/LST-OTA-012/LST-OTA-013. | Static original-worker-path comparison, build, then at least three consecutive real OTAs with `bulk_kb_s >= 45.0`, no Task WDT, one OTA reboot, no offset recovery, and post-OTA notify/audio readiness. |
| LST-OTA-015 | 2026-07-29 | A new BLE connection must make one bounded attempt to restore the original 1.0.3 7.5 ms active interval before OTA/audio transfer. If Windows rejects that preferred request, firmware may accept the existing 15 ms interval for the remainder of that connection and must not retry or create a status-571 loop. A later connection gets one new preferred attempt. This refines LST-OTA-011: 15 ms remains a valid fallback, but silently treating it as preferred before one bounded 7.5 ms attempt measured only 36.8 KiB/s and did not restore the demonstrated 1.0.3 level. | Static per-connection preference/fallback contract plus real OTA showing either accepted 7.5 ms or one rejection followed by stable 15 ms, no repeated 571, and `bulk_kb_s >= 45.0`. |
| LST-OTA-016 | 2026-07-29 | On the verified post-OTA notify fast path, opening a WinRT device/service object is not proof that its live device and GATT session are connected. Before registering terminal disconnect events, reusing the preserved CCCD, or writing `TYPE:READY`, Type must wait a bounded 1500 ms for that same target to reach `BluetoothConnectionStatus::Connected` and `GattSessionStatus::Active`. The outer fast-path window must include this bounded link-settle plus the existing stable-ready hold; it must not cancel a target that is transitioning normally. | Focused source/unit contract plus installed-Type log showing no heartbeat write begins while the target is disconnected, no premature refresh, and post-OTA notify ready within the fixed-time gate. |
| LST-OTA-017 | 2026-07-29 | If an otherwise-ready background capture disappears exactly during the pre-OTA `TYPE:OTA` handoff, Type must not spend tens of seconds opening a separate audio-control discovery path and must not abort permanently on the stale ready edge. Observability context handoff is best-effort through the active capture only; the required OTA lease handoff gets one bounded retry after a new notify-ready edge. If that retry still fails, abort before BEGIN and preserve pairing. | Focused handoff-race test plus installed-Type OTA started from an injected/stale ready edge, with bounded retry, no long audio-control discovery, and successful BEGIN or a clean pre-BEGIN abort. |
| LST-OTA-018 | 2026-07-29 | The post-OTA WinRT target must remain device-connected and GATT-active continuously for 250 ms before Type registers terminal disconnect handling, reuses the preserved CCCD, or writes `TYPE:READY`. A momentary connected snapshot is insufficient: real evidence showed the service-confirm probe releasing the link 53 ms later. If the target drops during this settle, keep polling within the existing bounded 1500 ms target window without issuing a GATT write; a timeout must release promptly rather than hold the serialized capture for the Windows native 30-second deadline. | Focused stable-link source/unit contract plus installed-Type log showing `post-OTA notify target stable` before heartbeat, no immediate disconnect/refresh, and notify ready inside the fixed-time gate. |
| LST-OTA-019 | 2026-07-29 | Preserve the device-side OTA inactivity deadline without reprogramming the `esp_timer` for every 500-byte DATA write. Each successful write updates the absolute deadline; an already-active one-shot timer remains armed and its callback re-arms only for the remaining time when necessary. This removes thousands of timer stop/start operations from the 1.0.3 flash hot path without changing the Type-side 10-second interruption watchdog, transfer window, dual lanes, queue depth, LED progress, or timeout semantics. | Static timer contract, firmware build, then at least three consecutive installed-Type real OTAs at `bulk_kb_s >= 45.0`, with no timeout, WDT, offset recovery, or reconnect regression. |
| LST-OTA-020 | 2026-07-29 | On the preserved-CCCD post-OTA fast path, the first `TYPE:READY` write is also a bounded 500 ms ATT-writability probe. If the device/session looks connected and stable but that probe does not complete, Type must abandon that target immediately, leave the restored CCCD enabled, skip synchronous `Close()` calls on the poisoned WinRT session, and open a fresh target without destructive pairing recovery. Real evidence showed explicit close of such a target holding the serialized capture for about 15 seconds. Normal non-OTA heartbeat timeouts and cleanup remain unchanged. | Focused timeout/cleanup source contract plus installed-Type log showing the poisoned target releases promptly, the next target sends `TYPE:READY`, pairing remains intact, and total non-transfer fixed time stays below 7000 ms. |
| LST-OTA-021 | 2026-07-29 | The successful post-reboot OTA service-confirmation probe must retain its `BluetoothLEDevice` handle until the audio notify target has successfully written `TYPE:READY`. Dropping the last probe handle before notify takeover lets Windows tear down the device-wide GATT connection immediately after declaring the OTA service reachable, producing a stable-looking but non-writable audio target and later service-discovery errors. The hold is generation-local, is replaced by a later successful confirmation, and is released after the fresh notify target proves ATT writability; it must not alter pairing or OTA throughput. | Focused handle-handoff source contract plus installed-Type log with service confirmation, notify target stable, first `TYPE:READY` success, no poisoned-target path, and non-transfer fixed time below 7000 ms. |
| LST-OTA-022 | 2026-07-29 | Post-reboot OTA service confirmation and the matching audio notify reopen must use uncached-only GATT discovery with a bounded 2000 ms discovery budget; neither may fall back to pre-reboot cached service or characteristic handles. A 58 ms `CachedFirst` service hit is not proof that the rebooted image is exposed, and a cached audio-control fallback after a 600 ms uncached timeout can look connected while every `TYPE:READY` write hangs. Normal warm startup keeps its existing persisted-bond cache policy. | Focused cache-policy contract plus installed-Type log showing uncached post-reboot service confirmation, uncached audio target discovery, first `TYPE:READY` success, and non-transfer fixed time below 7000 ms. |
| LST-OTA-023 | 2026-07-29 | Fix post-OTA reconnect at the BLE generation boundary rather than masking stale sessions with longer sleeps or retries. After FINISH has returned through GATT, firmware must explicitly terminate the current GAP connection and observe the disconnect (within a bounded reboot fallback) before restarting into the new image. Type must arm its old-device disconnect observer before FINISH and observe that peer-visible boundary before it releases the transfer target and begins post-reboot service confirmation. The normal one-reboot contract, existing bond, uncached new-generation discovery, `bulk_kb_s >= 45.0`, and non-transfer fixed time below 7000 ms remain protected. | Firmware static/build evidence for graceful OTA disconnect-before-restart, focused Type boundary-observer tests, then at least three installed-Type OTAs showing old-generation disconnect, one reboot, uncached new-generation confirmation, first `TYPE:READY` success, no poisoned-target path, and all existing speed/fixed-time gates. |
| LST-OTA-024 | 2026-07-29 | The OTA reboot boundary is complete only after Type observes the same Windows device identity transition from old-generation Connected to Disconnected and then new-generation Connected. An old-generation disconnect by itself is not permission to start uncached service discovery: real evidence showed firmware reconnecting about 2.35 seconds after reboot while a discovery started 0.5 seconds earlier became permanently stuck. Arm before FINISH, wait a bounded 4000 ms for the full transition, publish a one-shot new-generation-ready proof, and consume that proof to skip the legacy fixed reboot grace before uncached discovery. Do not replace this state transition with a longer sleep. | Focused source/unit contract for the three-phase observer and one-shot proof, plus installed-Type logs showing old disconnect, new connect, uncached service confirmation started only afterward, and the existing `<7000 ms` fixed-time gate. |
| LST-OTA-025 | 2026-07-29 | Once the pre-FINISH observer proves the same WinRT device object has crossed into the new connected generation, the successful OTA transfer target must hand that live device connection to the post-confirm hold instead of running its normal explicit `GattSession.Close`, service `Close`, and device `Close` cleanup. Those explicit closes remain mandatory for prepare failures, interrupted transfers, and targets that never prove the full generation transition. Real evidence showed the successful target destructor closing the just-reconnected generation immediately before uncached service confirmation. | Focused source/unit ownership contract plus installed-Type log showing generation confirmation followed by retained-device handoff, no intervening device disconnect/close, successful uncached service confirmation, and first `TYPE:READY` success. |
| LST-OTA-026 | 2026-07-29 | Type may request WinRT `ThroughputOptimized` at most once per foreground OTA generation. The initial request may wake an idle Windows link to 15 ms, after which firmware gets its one bounded opportunity to promote that connection to 7.5 ms. Reopening prepare/transfer GATT targets must not reissue the WinRT preference and overwrite a successfully established 7.5 ms interval back to 15 ms. Real serial evidence showed the sequence 15 ms -> 7.5 ms -> repeated Type request -> 15 ms, followed by only 17.5-26.7 KiB/s. Preserve the existing 400-chunk, dual-lane transfer and require `bulk_kb_s >= 45.0`. | Focused once-per-generation request contract plus real serial trace showing at most one Type WinRT request, no post-promotion 15 ms overwrite, and three consecutive installed-Type OTAs meeting the speed gate. |
| LST-OTA-027 | 2026-07-29 | A post-reboot `BluetoothConnectionStatus::Connected` event is still not permission to issue uncached GATT discovery. Type must arm the transfer target's `GattSession.MaxPduSizeChanged` handler before FINISH, ignore pre-disconnect events, and require a new-generation PDU/MTU event after the old-device disconnect and new-device reconnect. This is the observable ATT-ready boundary: real evidence showed uncached discovery started 18 ms after new Connected, before encryption/MTU/subscription restore, and the request then hung permanently. The observer remains bounded and must not be replaced by a fixed post-connect sleep. | Focused source/unit contract for the post-disconnect PDU event gate, plus installed-Type and firmware logs proving service discovery starts after the new boot's MTU exchange and reaches first `TYPE:READY`. |
| LST-OTA-028 | 2026-07-29 | Listener Type must not request WinRT `ThroughputOptimized` for the production OTA path. Real A/B evidence on the same installed build showed that this central-side preference intermittently overwrote the firmware-owned 7.5 ms active interval and produced 17.5-34.3 KiB/s transfers, while omitting it held the live link at 7.5 ms and transferred the same image at 52.2 KiB/s with zero offset recovery. Firmware owns the one bounded per-connection 7.5 ms request and may retain 15 ms only after a central rejection; Type target reopen must not issue a competing preference. This replaces the one-Type-request allowance in LST-OTA-026 while preserving the dual-lane, 32-entry, 400-chunk hot path. | Focused no-WinRT-request source/test contract, during-transfer serial samples proving stable 7.5 ms, and at least three consecutive installed-Type OTAs with `bulk_kb_s >= 45.0`, no offset recovery, one reboot, and post-OTA `TYPE:READY`. |
| LST-OTA-029 | 2026-07-29 | OTA performance evidence must measure protocol transfer time separately from the post-FINISH reboot/ATT generation boundary. `transferElapsedMs` is the Denzic OTA engine duration through accepted FINISH; old-disconnect/new-connect/new-PDU waiting is fixed reconnect work and must be included in `nonTransferFixedElapsedMs`. Do not hide reconnect delay inside transfer throughput or omit it from the `<7000 ms` fixed-time gate. | Focused result-accounting tests plus installed-Type evidence whose payload rate matches firmware `bulk_kb_s` and whose fixed-time field includes the observed generation-boundary wait. |
| LST-OTA-030 | 2026-07-29 | The new-generation ATT proof must come from a new `GattSession` created for the same device ID after the observed old Connected -> Disconnected -> new Connected transition. The pre-reboot session is generation-bound: real evidence showed its `MaxPduSize` resetting to 23 at disconnect and never emitting another MTU event after reconnect. Type must require the new session to become `GattSessionStatus::Active` with `MaxPduSize > 23` inside the existing bounded generation window, then transfer that session's ownership to post-confirm recovery. This replaces the pre-FINISH old-session PDU-handler mechanism in LST-OTA-027; the device transition observer remains armed before FINISH, and a fixed sleep is still not accepted as ATT readiness. | Focused source/test contract for post-Connected fresh-session creation, Active plus negotiated-PDU gating and ownership handoff, followed by installed-Type logs showing uncached service confirmation starts only after that proof and reaches first `TYPE:READY` within the fixed-time gate. |
| LST-OTA-031 | 2026-07-29 | Preserve the successful transfer target immediately after the pre-FINISH observer sees the complete old Connected -> Disconnected -> new Connected device transition. Do not wait for an MTU event on the pre-reboot session, and do not create an unattached replacement `GattSession` from the old device ID before transferring ownership: real evidence showed that path timing out and then the target destructor explicitly closing the already-reconnected device, after which every service probe stalled until Windows recovered. The retained target prevents that destructive close; an uncached service discovery on the retained new-generation connection is the ATT-ready proof, and only that successful discovery permits version confirmation and `TYPE:READY` recovery. This replaces the MTU/PDU mechanisms in LST-OTA-027 and LST-OTA-030 while retaining the bounded event-driven device transition from LST-OTA-024, the ownership rule from LST-OTA-025, and the prohibition on fixed post-connect sleeps. | Focused source/test contract proving immediate handoff on the full device transition, no fresh-session/PDU gate and no fixed grace, followed by installed-Type logs showing retained ownership before uncached service discovery, successful version confirmation and first `TYPE:READY` within the fixed-time gate. |
| LST-OTA-032 | 2026-07-29 | After the complete old-disconnected/new-connected device transition, keep the successful transfer target alive and prove new-generation ATT readiness by completing an uncached read of that target's existing 24-byte OTA status characteristic inside the existing bounded generation window. Do not start a new WinRT service enumeration immediately after `Connected`: real serial/Type correlation showed firmware encryption, restored subscriptions and MTU completing about 340 ms later, while service discovery started at 11 ms intermittently poisoned the WinRT operation for 2-9 seconds. A successful retained-target status read is the protocol-service confirmation and may directly confirm the expected image transfer before `TYPE:READY`; service discovery remains only a bounded diagnostic fallback when that read cannot complete. This refines the ATT-proof mechanism in LST-OTA-031 without adding a fixed sleep or changing transfer throughput. | Focused source/test contract for the retained uncached status read and direct confirmation path, plus at least three consecutive installed-Type logs showing status proof after new Connected, no fast service enumeration on the success path, first `TYPE:READY`, and total non-transfer fixed time below 7000 ms. |
| LST-OTA-033 | 2026-07-29 | Make new-generation ATT readiness an explicit OTA protocol event. The OTA status characteristic is readable and notifiable; Type registers its value-change handler and enables the status CCCD before FINISH, while firmware emits the 24-byte status value after a status subscription is restored and again when the new connection's MTU is established. Type ignores pre-disconnect status notifications and accepts only a valid status notification after the old-generation disconnect, together with the subsequent new `Connected` transition. This event is the protocol-service confirmation and permits direct version-transfer confirmation before `TYPE:READY`. Do not reuse a pre-reboot characteristic for a post-reboot read: real evidence showed that read timing out despite firmware already restoring subscriptions and MTU. This replaces the read mechanism in LST-OTA-032 while preserving its no-sleep, no-immediate-service-enumeration and throughput requirements. | Firmware static/build evidence for the status notify property and subscribe/MTU emission, focused Type handler/ordering tests, then at least three consecutive installed-Type OTAs showing old disconnect, new-generation status notification, new Connected, direct confirmation, first `TYPE:READY`, `bulk_kb_s >= 45.0`, no offset recovery, one reboot and fixed time below 7000 ms. |
| LST-OTA-034 | 2026-07-29 | Carry the explicit post-reboot `DOTA_READY_V1` event over the already-existing diagnostic data notification characteristic, not by changing the OTA status characteristic properties or the GATT schema revision. Type opens and subscribes to this stable diagnostic notify surface before FINISH, ignores its initial/pre-disconnect marker, and accepts only the marker emitted after the old-generation disconnect plus the new `Connected` transition. Firmware emits the marker when the diagnostic CCCD is subscribed/restored and after MTU establishment. Real factory-flash evidence showed that changing the OTA status property forced Windows GATT cache re-enumeration and left the bonded firmware connection secure while WinRT sessions stayed inactive; that regression is prohibited. This replaces only the transport surface in LST-OTA-033 and retains its explicit event ordering, direct confirmation, no fixed sleep and performance gates. | Static/build evidence proving the original OTA status flags and schema revision remain unchanged while the diagnostic marker is emitted on subscribe/MTU; focused Type ordering/CCCD tests; then three consecutive installed-Type OTA passes with the marker, one reboot, first `TYPE:READY`, speed and fixed-time gates. |
| LST-OTA-035 | 2026-07-29 | A pre-reboot WinRT `GattCharacteristic` object is generation-bound even when Windows restores its bonded CCCD after reboot: firmware evidence showed both restored-subscription and MTU `DOTA_READY_V1` notifications succeeding while the old Type object delivered neither event. After the observed old-disconnected/new-connected boundary, Type must open a fresh cached-only diagnostic target on the unchanged schema, register the new-generation data handler, enable its CCCD, write the explicit `{"op":"ota_ready"}` challenge, and accept only the matching `DOTA_READY_V1` response. The fresh challenge/response is the ATT-ready and transferred-image confirmation; it remains inside the existing bounded generation window and must not start uncached OTA-service enumeration or add a fixed sleep. This replaces the pre-FINISH old-object delivery assumption in LST-OTA-034 while preserving the stable diagnostic surface and all speed/fixed-time gates. | Firmware build/static evidence for the diagnostic challenge response, focused Type tests for post-connected fresh cached-only target ordering, then three consecutive installed-Type OTA passes with challenge/response, one reboot, first `TYPE:READY`, `bulk_kb_s >= 45.0`, no offset recovery and fixed time below 7000 ms. |
| LST-OTA-036 | 2026-07-29 | Once `TYPE:OTA` establishes the device-side preparation lease, unrelated Type activity such as `VREC:PROCESSING:STOP`, processing status, or lossless-audio capability writes must refresh host presence without clearing that lease. Only a later explicit normal `TYPE:READY`/`TYPE:HB` recovery or lease expiry may release a failed pre-BEGIN handoff; beginning OTA keeps the power blocker as the transfer owner. Real serial evidence showed `VREC:PROCESSING:STOP` clearing the lease, re-enabling voice activation and allowing a new automatic recording to block five BEGIN attempts, cutting measured throughput to 42.3 KiB/s. | Focused state/static test proving non-recovery Type activity preserves the OTA lease and voice monitoring stays disabled through BEGIN, firmware build, then installed-Type OTA traces with no post-handoff automatic recording/BEGIN retry and the existing throughput gate. |
| LST-OTA-037 | 2026-07-29 | The fresh post-reboot diagnostic object must reuse the bonded diagnostic CCCD that Windows and firmware already restored; it must not issue another descriptor write. Real run evidence showed firmware restoring the diagnostic subscription and emitting both connect/MTU markers, while Type's redundant new-object CCCD write consumed the final 662 ms of the bounded generation window and timed out. After new `Connected`, Type registers the fresh object's handler and immediately writes `{"op":"ota_ready"}`; the exact `DOTA_READY_V1` response proves that the restored subscription and ATT path are live. This removes only the redundant CCCD write from LST-OTA-035 and preserves its fresh cached-only object, explicit challenge, no-sleep and fixed-time requirements. | Focused source test proving no post-connected diagnostic CCCD write and challenge ordering, then three installed-Type OTA passes with firmware restored-subscription evidence, challenge response, speed/fixed-time gates and first `TYPE:READY`. |
| LST-OTA-038 | 2026-07-29 | A successful fresh diagnostic challenge proves that the unchanged new-generation GATT database is live and writable, so the matching audio-notify takeover must use the same verified address with cached-only service/characteristic lookup and the Windows-restored audio CCCD. It must not immediately issue the old uncached audio-service enumeration: real evidence showed diagnostic challenge success in 393 ms followed by repeated 2000 ms uncached audio discovery timeouts and a 33-second `TYPE:READY`. Uncached discovery remains a later diagnostic/recovery path only when explicit new-generation proof is absent. | Focused cache-policy test for challenge-proven audio takeover, then installed-Type OTA logs showing cached target open, restored CCCD reuse, first `TYPE:READY` and total non-transfer fixed time below 7000 ms. |
| LST-OTA-039 | 2026-07-29 | Tighten the installed-Type OTA performance acceptance threshold from `>=45.0 KiB/s` to strictly `>50.0 KiB/s` sustained protocol `bulk_kb_s`. Do not satisfy this by changing accounting, excluding protocol work, shrinking the image, or weakening reliability/reconnect gates; retain the same 1,387,936-byte image, 400-chunk window, dual lanes, 32-entry worker pipeline, zero offset recovery, one reboot and `<7000 ms` non-transfer fixed-time requirements. This performance threshold supersedes the lower gates in LST-OTA-004/LST-OTA-011 and later entries. | Harness threshold and focused static contract, followed by at least three consecutive installed-Type real OTA passes where every run reports `bulk_kb_s > 50.0` with the unchanged image and all reliability/reconnect gates. |
| LST-OTA-040 | 2026-07-29 | Budget the event-driven reboot transition and its new-generation ATT challenge separately. The old-disconnected/new-connected device transition keeps its bounded 4000 ms deadline; after `Connected` is observed, the fresh cached diagnostic `ota_ready` challenge gets its own bounded 1500 ms operation deadline. Real runs showed the physical reboot/reconnect consuming 3124-3209 ms, leaving only 753 ms of the shared deadline and intermittently timing out a valid challenge. This is not a fixed grace or sleep: both stages complete immediately on their events, and the aggregate OTA non-transfer fixed time must still remain below 7000 ms. | Focused deadline-source test plus installed-Type logs with transition and challenge elapsed times, challenge response, direct confirmation and aggregate fixed time below 7000 ms. |
| LST-OTA-041 | 2026-07-29 | Make the `>50.0 KiB/s` gate stable by removing per-500-byte Flash-call overhead at the device storage boundary. Preserve Type's 500-byte ATT chunks, dual lanes, 32-entry worker queue, 400-chunk window, core offset/CRC accounting and worker ownership, but coalesce four already-ordered chunks into one bounded 2000-byte `esp_ota_write`. Flush the remainder before every status/SYNC observation and before FINISH; discard it on ABORT/reset. A buffered chunk is never exposed as device-confirmed progress before its physical flush, and any flush failure aborts the session. The 2000-byte buffer stays in internal static memory and must not move the Flash operation or its source buffer onto PSRAM. | Static batching/flush/error contract, firmware build, then three consecutive installed-Type OTA passes over the unchanged image with `bulk_kb_s >50.0`, zero offset recovery, no WDT and all reconnect gates. |
| LST-OTA-042 | 2026-07-29 | After the observed old-disconnected/new-connected boundary, a `DOTA_READY_V1` notification received by the newly registered handler on the fresh cached diagnostic object is sufficient new-generation ATT proof when it comes from the bonded CCCD that Windows restored. Type must not clear this marker after registering the handler. It first waits a bounded 300 ms for the restored-subscription/MTU marker and sends the existing `{"op":"ota_ready"}` write only as a fallback when no marker arrives; both paths share the separate 1500 ms challenge deadline. Real evidence showed Type discarding the restored marker and then timing out a redundant write after 1362 ms even though firmware had already emitted the marker. This refines the immediate-challenge ordering in LST-OTA-035/LST-OTA-037 while preserving the fresh object, exact marker validation, no descriptor write, no fixed reboot grace and `<7000 ms` fixed-time gate. | Focused source tests for marker-first and challenge fallback ordering, followed by three consecutive installed-Type OTA passes with direct confirmation, cached audio takeover, first `TYPE:READY`, strict speed and fixed-time gates. |
| LST-OTA-043 | 2026-07-29 | The firmware must make the restored-subscription `DOTA_READY_V1` readiness event observable to a fresh post-Connected WinRT characteristic even when Windows completes CCCD/MTU restoration before Type can register that fresh object's handler. After an enabled diagnostic subscription or MTU event, emit the marker immediately and retry it on the NimBLE host event queue at 120 ms intervals for at most four additional attempts; cancel the bounded retry sequence on disconnect or disabled subscription. This is a finite event retransmission, not a Type-side sleep, GATT schema change, descriptor rewrite, or unbounded periodic notification. It must not touch the OTA Flash hot path or throughput. | Static callout/cancel/count contract, firmware build, and three consecutive installed-Type OTA passes where the fresh marker confirms directly, no fallback diagnostic write or uncached service probe runs, fixed time is below 7000 ms and speed remains strictly above 50.0 KiB/s. |
| LST-OTA-044 | 2026-07-29 | Replace the post-reboot diagnostic-notification proof with a fresh cached audio target on the observed new device generation. Real evidence showed that neither the original nor a newly created WinRT diagnostic characteristic receives the firmware's successfully sent restored-CCCD marker, including bounded retransmissions, while the same generation's cached audio control reaches `TYPE:READY` in 3 ms. Type opens the exact verified address with cached-only audio notify/control handles, waits for device Connected plus GATT Active to remain stable for 250 ms, and writes `TYPE:OTA` with response as a bounded ATT-writability probe. Success directly confirms the transferred image and retains the device/session for the normal preserved-CCCD audio takeover; no diagnostic CCCD write, notification wait, uncached service enumeration, pairing action or fixed grace is allowed. The probe has its own 1500 ms deadline after the 4000 ms generation transition and does not alter OTA throughput. This replaces LST-OTA-035 through LST-OTA-043 as the active ATT-proof mechanism; the stable diagnostic schema and marker remain available for diagnostics but are not release-critical. | Focused cached-audio/stable-link/probe/ownership source tests, then three consecutive installed-Type OTA passes with direct confirmation, first `TYPE:READY`, fixed time below 7000 ms, speed strictly above 50.0 KiB/s, zero offset recovery and one reboot. |
| LST-OTA-045 | 2026-07-29 | Stabilize the strict `>50.0 KiB/s` gate across the observed Windows link scheduling variance by coalescing eight ordered 500-byte chunks into each 4000-byte internal-memory Flash call instead of four. Preserve all flush-before-status/FINISH, physical-progress, error, dual-lane, queue and window rules from LST-OTA-041. The resulting internal boot heap must still initialize every production task successfully. Once the fresh audio-control probe has already held the new generation stable and proved ATT writability, begin the preserved-CCCD audio takeover immediately; remove the redundant 250 ms post-confirm sleep so fixed work remains below 7000 ms without weakening the 250 ms stable-link checks themselves. | Static 8-chunk/internal-buffer and no-redundant-settle contracts, firmware build with boot heap/task evidence, then three consecutive installed-Type OTA passes over the unchanged image with speed strictly above 50.0 KiB/s, fixed time below 7000 ms, zero offset recovery, one reboot and first `TYPE:READY`. |
| LST-OTA-046 | 2026-07-29 | The fresh cached audio-control pre-confirm probe uses the characteristic's native WriteWithoutResponse mode for `TYPE:OTA`, not a blocking response write. Real A/B evidence showed the response write intermittently hanging for 918 ms immediately after a stable new-generation GATT target, while the same target's later response-bearing `TYPE:READY` completed in 5 ms. Direct image confirmation therefore requires successful OTA FINISH, the full old-disconnected/new-connected boundary, a fresh exact-address cached audio target stable for 250 ms, and successful WWR submission; the overall OTA result still requires the subsequent response-bearing `TYPE:READY`, which remains the definitive live-device proof and may not be weakened or skipped. This refines only the probe write mode in LST-OTA-044 and preserves its 1500 ms outer bound, ownership handoff, pairing and fixed-time rules. | Focused source test for WWR probe plus response-bearing final TYPE:READY, then three consecutive installed-Type OTA passes with direct confirmation, final TYPE:READY, fixed time below 7000 ms and speed strictly above 50.0 KiB/s. |
| LST-OTA-047 | 2026-07-29 | Raise the installed-Type sustained OTA acceptance target from strictly above 50.0 KiB/s to strictly above 60.0 KiB/s on every run. Keep the same complete firmware image, protocol timing definition, dual lanes, 400-chunk window, 32-entry queue, 8-chunk Flash batch, zero offset recovery, one reboot, final response-bearing `TYPE:READY` and `<7000 ms` fixed-time gate. Do not meet this target by changing accounting, omitting protocol work or accepting a transient peak. This supersedes the speed threshold only in LST-OTA-039 and later entries. | Harness threshold plus three consecutive installed-Type OTA passes where every sustained protocol result is strictly above 60.0 KiB/s and all reliability/reconnect gates pass. |
| LST-OTA-048 | 2026-07-29 | Do not open a duplicate cached audio target solely for a pre-confirm probe after the complete same-device old Connected -> Disconnected -> new Connected transition. Real evidence showed the WWR itself taking 3 ms but its duplicate target open/stable sequence adding about 526 ms, followed immediately by the real preserved-CCCD audio target opening again and pushing fixed time to 7433 ms. Retain the successful transfer target immediately at the complete generation boundary and skip legacy service confirmation; the overall OTA result is valid only after the real fresh exact-address audio target remains Connected/GATT Active for 250 ms and completes the response-bearing `TYPE:READY`. Thus `TYPE:READY`, not an inferred pre-probe, is the definitive ATT and automatic-reconnect proof. This replaces the pre-confirm audio probe in LST-OTA-044/LST-OTA-046 without weakening the old/new generation boundary, final response write, pairing preservation or fixed-time gate. | Focused source test for immediate generation handoff plus mandatory final response-bearing TYPE:READY, then three consecutive installed-Type OTA passes with speed strictly above 60.0 KiB/s, fixed time below 7000 ms, zero offset recovery and one reboot. |
| LST-OTA-049 | 2026-07-29 | Keep the owner-accepted `v1.0.2` EC11 knob-ring and board-edge OTA renderers unchanged. The separate steady cyan status progress LED remains non-breathing and stepped, but its first/minimum brightness step starts at 1% and rises monotonically to the existing 90% peak. A yellow LED6 after the OTA reboot is a real transient WARN/error-domain signal and must be diagnosed and fixed at its startup/recovery trigger; do not hide it by suppressing the WARN renderer. | Static LED contract for a 1%-90% monotonic stepped status mapping with unchanged EC11/edge renderers, plus a real OTA reboot serial trace identifying and eliminating the post-boot WARN source. |
| LST-OTA-050 | 2026-07-29 | Stabilize the strict `>60.0 KiB/s` target at the device Flash boundary without increasing internal-memory use or changing the OTA path. Replace the 4000-byte/eight-whole-chunk write grouping with an exact 4096-byte internal buffer; the append path may split one ordered 500-byte ATT payload across adjacent batches so every full `esp_ota_write` matches one ESP32 Flash erase sector. Preserve flush-before-status/FINISH, physical-progress accounting, CRC/order/error behavior, 500-byte ATT chunks, dual lanes, 32-entry queue and 400-chunk host window. | Static sector-alignment/split-append/internal-buffer contract, firmware build and boot-task evidence, then three consecutive full-image installed-Type OTA passes strictly above 60.0 KiB/s with zero offset recovery and all reconnect gates. |
| LST-OTA-051 | 2026-07-29 | Reject and replace the split-chunk 4096-byte experiment in LST-OTA-050: real transfer evidence showed one offset recovery, 121 duplicate writes and only 39.8 KiB/s, so storage batching must keep each 500-byte protocol chunk whole. Fix the post-reboot LED6 WARN at its memory root: FreeRTOS task stack depth is measured in `StackType_t` words, and the OTA worker's `4096` value silently reserved 16 KB of permanent internal DMA-capable RAM before audio initialization. Use explicit 2048-word (8 KB) worker/reboot task depths, log worker stack high-water after FINISH, and use the recovered memory to batch sixteen whole ordered chunks into bounded 8000-byte internal Flash calls. Audio PDM DMA and every production task must initialize after OTA reboot; no WARN suppression is allowed. | Static stack-unit/whole-chunk/batch contract, firmware build, boot log with successful audio DMA plus no `audio_capture_start_failed`, worker high-water evidence, then three complete installed-Type OTA passes strictly above 60.0 KiB/s with zero offset recovery and all reconnect gates. |
| LST-OTA-052 | 2026-07-29 | Refine the stack correction in LST-OTA-051 from real hardware evidence: a 2048-word/8 KB worker panicked during the first OTA SYNC, so the Flash worker requires 3072 words/12 KB; the one-shot reboot task may remain 2048 words. This still recovers 4 KB of permanent internal DMA-capable RAM versus the original accidental 4096-word/16 KB worker and must allow PDM DMA initialization. Restore the proven whole-chunk eight-by-500-byte Flash batch until worker high-water and boot evidence demonstrate a safe larger batch; a panic, audio DMA failure, offset recovery or partial reboot is an automatic failure. | Static 3072-word worker/eight-whole-chunk contract, boot and worker high-water logs, then full OTA machine gates with no panic, audio error or offset recovery. |
| LST-OTA-053 | 2026-07-29 | Replace the incorrect word-based stack interpretation in LST-OTA-051/LST-OTA-052 with the verified ESP-IDF contract and panic evidence. ESP-IDF's task depth argument is bytes, while the static backing buffer is a `StackType_t[]`: the worker must keep a 4096-byte task depth and allocate exactly `4096 / sizeof(StackType_t)` elements. The old `StackType_t[4096]` reserved 16 KB while only exposing a 4096-byte stack; lowering the depth caused a confirmed `ble_ota_worker` stack-overflow panic. The corrected backing allocation recovers 12 KB of permanent internal DMA memory without shrinking the live worker stack. Keep eight whole 500-byte Flash batches until the corrected worker high-water is measured. | Static byte-depth/backing-size contract, build, USB/OTA boot logs with no stack overflow or safe mode and successful audio PDM DMA, worker high-water evidence, then the full consecutive OTA gates. |
| LST-OTA-054 | 2026-07-29 | Correct the backing-size conclusion in LST-OTA-053 using the local ESP-IDF Xtensa port source: `StackType_t` is `uint8_t`, so the original `StackType_t[4096]` was already a 4096-byte stack and must remain unchanged; the 2048/3072 reductions caused the confirmed stack-overflow panic. The post-OTA LED6 WARN instead comes from the added 4000-byte static Flash batch consuming internal DMA-capable RAM before PDM initialization. Allocate the eight-whole-chunk batch only at OTA BEGIN with `MALLOC_CAP_INTERNAL | MALLOC_CAP_8BIT`, after audio DMA initialization, and free it on BEGIN failure, FINISH, ABORT and core reset. Flash payload must never move to PSRAM, and allocation failure must reject BEGIN cleanly. | Static internal dynamic-allocation/all-exit release contract, build, boot log with successful PDM DMA and no LED6 audio error, then full OTA runs proving batch allocation succeeds, stack high-water remains safe, throughput and all reliability gates pass. |
| LST-OTA-055 | 2026-07-29 | The OTA/audio mutual exclusion in LST-OTA-010 applies to the physical PDM capture hot path as well as recording sessions and voice activation. Once BEGIN is accepted, suspend idle I2S capture for the device-side OTA session so unused audio DMA interrupts and frame processing do not contend with the worker-owned Flash path. A rejected BEGIN, ABORT, timeout, or verification failure must restore idle capture without reboot; successful FINISH may keep capture suspended only until the intentional OTA reboot. Do not deinitialize or reallocate the boot-proven audio DMA resources, and do not weaken recording availability after recovery. | Static suspend/resume/all-exit contract, firmware build, real OTA serial trace with capture suspended during transfer and restored after injected ABORT, then the existing three-run speed/reboot/reconnect/audio gates. |
| LST-OTA-056 | 2026-07-29 | Refine LST-OTA-055 after real internal-heap evidence: merely disabling I2S left its DMA allocation resident, so the 4000-byte OTA batch reduced internal free memory to 2723 bytes and both active-link scheduling attempts failed with `ESP_ERR_NO_MEM`; the otherwise healthy transfer remained at 44.1 KiB/s. After the OTA audio lock has stopped the capture task, delete only the I2S RX channel for the active OTA session and reuse the released internal DMA capacity for the existing BLE/Flash path. Keep the AFE model/tasks and all boot-proven audio state allocated. BEGIN failure, ABORT, timeout, or verification failure must recreate and enable the same I2S configuration before releasing the audio lock; successful FINISH recreates it through the one intentional reboot. This replaces only the no-DMA-deinitialization clause in LST-OTA-055. | Static I2S-only release/recreate ordering contract, build, injected-ABORT capture proof, and three complete OTA runs with no active-link `NO_MEM`, strict speed/reconnect gates, successful post-reboot PDM capture and no LED6 WARN. |
| LST-OTA-057 | 2026-07-29 | Real A/B evidence with the same current firmware showed a fresh installed-Type process improving sustained OTA from 44.1 to 51.1 KiB/s, while 25.2 seconds of a 26.6-second transfer remained in host control/WWR-drain time. Keep the firmware's 32-entry receive queue, dual lanes, 400-chunk windows, whole 500-byte chunks and eight-chunk Flash batch unchanged. Raise only the Windows host's outstanding WinRT WWR depth from 32 total (16 per lane) to 48 total (24 per lane); do not retry the historically regressive 64-total/32-per-lane shape. Accept the change only if real full-image evidence is strictly above 60.0 KiB/s with zero offset recovery and no reconnect/reliability regression; otherwise reject it and continue root-cause work. | Focused host-depth static/unit contract followed by installed-Type real OTA A/B evidence reporting transfer/control/data/status timing, speed, recovery count, reboot count and final response-bearing `TYPE:READY`. |
| LST-OTA-058 | 2026-07-29 | Reject the 48-total/24-per-lane Windows WWR-depth experiment in LST-OTA-057. On the same 1,389,808-byte image it preserved zero offset recovery, one reboot and 6314 ms fixed time, but regressed sustained transfer to 29.9 KiB/s and increased host control/WWR-drain time from 25.2 to 43.8 seconds. Restore the accepted 32-total/16-per-lane host depth and do not use deeper WinRT queues as the speed fix. | Installed-Type artifact `ota-root-fix16-pipeline48-run1c.json` plus Type log timing, followed by the restored 32-depth source/unit contract. |
| LST-OTA-059 | 2026-07-29 | Fix the per-generation WinRT throughput decay at GATT-session ownership. Every successful `SetMaintainConnection(true)` request must be balanced with `SetMaintainConnection(false)` when that target is released or handed off. This includes cleanup paths that intentionally skip synchronous `Close()` because a poisoned post-OTA target can block there for about 15 seconds. Releasing an old generation's maintain request must not close or disturb the fresh notify session that owns final `TYPE:READY`; keep the accepted 32-total/16-per-lane WWR depth and all reconnect boundaries unchanged. | Focused ownership/static tests plus repeated installed-Type OTAs in one process showing no generation-over-generation `control_write_ms` growth, strict `>60.0 KiB/s`, one reboot and final `TYPE:READY`. |
| LST-OTA-060 | 2026-07-29 | Remove the deterministic start-of-bulk scheduling loss identified by per-window timing. At accepted BEGIN, firmware must invoke the existing bounded active-link request immediately instead of waiting the generic 750 ms deferred-audio delay; the request implementation may keep its existing asynchronous fallback when parameters are not yet applied and must still obey the per-connection rejection backoff in LST-OTA-015. Type must not add a fixed 150 ms quiet period after WinRT reports the WWR pipeline submitted: the following response-bearing SYNC already provides the ordered air/link drain and the status read remains the confirmed-offset proof. Preserve 500-byte chunks, dual lanes, 32-total/16-per-lane host depth, 32-entry device queue, 400-chunk windows, eight-whole-chunk Flash batches and the 10-second rolling error/no-progress semantics. | Per-window installed-Type log separating host-handle flush from response-bearing SYNC, static immediate-BEGIN/no-fixed-quiet contracts, firmware and Type builds, then three consecutive full-image installed-Type OTAs strictly above 60.0 KiB/s with zero offset recovery and all reboot/reconnect/audio gates. |
| LST-OTA-061 | 2026-07-29 | Keep Windows' firmware-update radio scheduling request alive for the entire real OTA protocol transfer. Microsoft documents `BluetoothLEDevice.RequestPreferredConnectionParameters(ThroughputOptimized)` specifically for firmware updates and requires the returned closable request to remain in force for the update duration, then be restored/released. Type must create the request from the exact bulk target's `BluetoothLEDevice`, retain it inside `ListenerOtaV1Transport`, and explicitly `Close()` it on every success, error and ABORT exit. This host-radio scheduling lease complements rather than replaces firmware's bounded per-connection 7.5 ms request; it must not change the 32-total/16-per-lane WWR depth, window, chunking, Flash path, timeout accounting or reconnect ownership. This replaces the earlier interpretation that Type must never request `ThroughputOptimized`, after real 7.5 ms/2M/MTU517 traces still measured only 33.5-34.0 KiB/s while the host request was absent. | Focused source/unit contract proving request ownership spans `denzic_ota_core::transfer` and `Close()` is guaranteed by transport drop, Type build/install identity, then same-image installed-Type A/B and three consecutive full OTAs strictly above 60.0 KiB/s with zero offset recovery, one reboot, `<7000 ms` fixed time and final response-bearing `TYPE:READY`. |
| LST-OTA-062 | 2026-07-29 | Reject the retained WinRT `ThroughputOptimized` lease experiment in LST-OTA-061. The exact installed build logged request status `Success`, retained it across the full protocol and explicitly released it afterward, but the same 1,389,744-byte image regressed from the immediately preceding 34.0 KiB/s to 30.7 KiB/s, with zero offset recovery but a fixed-time reconnect failure. Restore firmware-only bounded connection-parameter ownership while continuing root-cause work from the captured link evidence; do not ship or retry the Type-side lease as the speed fix. | Installed-Type artifact `ota-throughput-lease21-ab-run1.json`, matching Type retained/released logs and serial link trace, followed by the no-lease focused source contract and a rebuilt current Type package. |
| LST-OTA-063 | 2026-07-29 | Treat WinRT `GattSession.SetMaintainConnection` as device-session state during the intentional prepare-target to secure-bulk-target handoff, not as an independently reference-counted lease on each wrapper. Preserve the prepare session's `true` state across the 350 ms encryption settle and secure reopen; after the fresh target has successfully established its own maintained session, discard only the superseded wrapper without an intervening `false` or synchronous `Close`, because either call can collapse the shared radio state immediately before bulk. If secure reopen never succeeds, the retained prepare session must still execute `false` plus `Close`; the fresh bulk target remains responsible for the normal final `false` plus `Close`. This supersedes only the per-wrapper balancing interpretation in LST-OTA-059 at this overlapping ownership boundary and must not weaken failure cleanup, post-reboot `TYPE:READY`, pairing or reconnect behavior. Accept only if same-image installed-Type A/B materially restores sustained throughput without recovery or reconnect regression; otherwise reject and revert. | Focused ownership-ordering test proving retain-before-old-target-drop, transfer only after fresh maintained session exists, and failure-path `false` plus `Close`; then current MSI identity and same-image real OTA artifact with per-control timing, zero offset recovery, one reboot and final response-bearing `TYPE:READY`. |
| LST-OTA-064 | 2026-07-29 | Pending-verify must distinguish a definitive BLE stack startup failure from an encrypted-link readiness signal that is merely still in flight. A failed POST, failed keyboard startup, or failed BLE stack/HID startup remains an immediate rollback condition. When those startup checks pass but the bonded encrypted BLE link has not yet completed, keep the image pending and wait for the existing secure-link callback; if no host connects, the existing 60-second clean-uptime fallback may promote the successful BLE startup signal. Never roll back only because Windows encryption completed a few hundred milliseconds after `app_main` recorded startup checks. This fixes the observed `reason=0x00000002` race that booted the transferred image once, then rolled back and produced a second boot light. | Focused pending-verify ordering/static contract, firmware build, then real OTA serial evidence where startup self-check precedes encryption yet logs a deferred decision, the later secure link marks the new image valid, there is exactly one post-FINISH reboot, no rollback, final response-bearing `TYPE:READY`, and the existing speed/audio gates still pass. |
| LST-OTA-065 | 2026-07-30 | A post-boot PHY-complete event may arrive before Windows finishes the bonded encryption ceremony. Do not issue the synchronous `ble_gap_set_data_len` HCI command from that pre-secure window: the observed controller timeout held the NimBLE host for 20 seconds, then its reset startup dereferenced a missing HCI ACK and crashed with `LoadProhibited`. DLE remains required for the accepted 2M/251-byte path, but it must be requested only after the tracked connection is secure, and repeated callers must coalesce while one DLE request is pending instead of issuing another HCI command. A later secure `TYPE:READY`/active-audio path owns the deferred request. Do not hide the crash with a reconnect timeout increase or disable DLE/2M throughput. | Focused secure-before-DLE and one-in-flight static contract, firmware build, then three consecutive same-image installed-Type OTA runs strictly above 60.0 KiB/s with one reboot, no HCI timeout/NimBLE reset/panic/rollback, fixed time below 7000 ms, and final response-bearing `TYPE:READY`; final post-OTA audio must still report the 251-byte DLE path and complete normally. |
| LST-OTA-066 | 2026-07-30 | A real owner OTA measured only 33.3 KiB/s even though the device advertised `active_link_confirmed=true`. That flag must represent the proven full-speed transport, not merely a latency-zero connection as slow as 30 ms. Unlock the 400-chunk active window only after the bonded link is secure, the connection interval is within the accepted 7.5-15 ms range, 2M PHY is active in both directions, and DLE reports at least 251 transmit octets. Until then, retain the bounded inactive-link window while the existing active-link promotion proceeds. Do not change the byte-count timing denominator or lower the strict sustained `bulk_kb_s > 60.0` gate. | Static full-speed-ready predicate plus a freshly built package and three consecutive complete installed-Type OTAs, each transferring the same full image strictly above 60.0 KiB/s with zero offset recovery, one reboot, no HCI/NimBLE reset/OOM/rollback, fixed time below 7000 ms and final `TYPE:READY`. |
| LST-OTA-067 | 2026-07-30 | The current Listener Type MSI and Listener Firmware OTA ZIP are owner-facing root artifacts and must be regenerated together after the final source change. Publish them directly under `C:\Users\Billy\Desktop\Denzic\Listener` as `ListenerType_1.0.3_x64_en-US.msi` and `ListenerFirmware_1.0.3_ota.zip`, replacing the older files with those names. The root hashes must exactly match the newly built source artifacts; the firmware ZIP manifest/image identity must match the package used by the final real OTA machine loop, and the Program Files executable must match the new MSI payload. | Fresh Firmware/Type builds, manifest and SHA-256 comparison across source artifacts/root artifacts/installed executable, root package listing with no older Listener MSI/ZIP aliases, and the LST-OTA-066 real OTA machine loop using the root ZIP. |
| LST-OTA-068 | 2026-07-30 | Refine the full-speed DLE proof in LST-OTA-066 after the repeated-run slowdown was reproduced. A 251-byte data length with only 1590 microseconds of transmit time is not the accepted fast link: require both at least 251 transmit octets and the requested 2120-microsecond transmit time. If Windows renegotiates the active OTA link down to 1590 microseconds during transfer, immediately withdraw `active_link_confirmed`, asynchronously request the existing 251-byte/2120-microsecond DLE target again, and restore the 400-chunk window only after the completion event proves the full target. Do not issue the HCI command synchronously from the GAP callback, do not alter the 32-total/16-per-lane host pipeline, and do not hide the slowdown by selecting only favorable runs. | Static octet-plus-time readiness and asynchronous downgrade-recovery contract, firmware build, then consecutive same-root-package OTA logs showing no mid-transfer false-fast interval, strict `>60.0 KiB/s`, zero offset recovery and the existing fixed-time/reboot/reconnect/audio gates. |
| LST-OTA-069 | 2026-07-30 | The bounded host window used while full-link confirmation is absent must be 128 chunks rather than 64. Real speed27 evidence showed the firmware scheduling the DLE restore within 20 ms, while the Windows controller took about 6 seconds to complete it; the 64-chunk window added seven response-bearing SYNC/status round trips and reduced an otherwise healthy zero-recovery run to 59.0 KiB/s. A 128-chunk bounded window halves that control overhead while remaining below the 400-chunk confirmed-link window and retaining the same 32-total/16-per-lane instantaneous WinRT WWR depth. Do not change chunk size, the confirmed 400 window, device queue/Flash batch, timeout semantics or the strict speed denominator. | Focused constant/unit contract, rebuilt and installed Type MSI identity, then repeated same-root-package runs including at least one observed 1590-to-2120 DLE recovery, all strictly above 60.0 KiB/s with zero offset recovery and existing reliability gates. |
| LST-OTA-070 | 2026-07-30 | Replace the 128-chunk bounded-window experiment in LST-OTA-069 with 200 chunks. A real zero-recovery speed28 run with two separate DLE recovery intervals still needed 17 status reads and measured 59.0 KiB/s, so 128 did not remove enough response-bearing control overhead. At 500 bytes per chunk, the 200-chunk bounded window covers 100 KB and remains safely below both the confirmed 400-chunk window and the 10-second progress watchdog even at the observed degraded radio rate; instantaneous WinRT WWR depth remains 32 total/16 per lane. Accept 200 only through repeated strict speed/reliability evidence; do not increase it again without new queue/watchdog proof. | Focused 200/400/32 constant contract, rebuilt and installed Type MSI identity, then repeated same-root-package runs including observed DLE recovery, all strictly above 60.0 KiB/s with zero offset recovery and existing reliability gates. |
| LST-OTA-071 | 2026-07-30 | Supersede the 2120-microsecond requirement in LST-OTA-068 and reject the bounded-window experiments in LST-OTA-069/LST-OTA-070. Bluetooth SIG timing material gives 2120 microseconds as the maximum 251-octet LE 1M packet time and 1064 microseconds for LE 2M; therefore Windows' observed 1590-microsecond value fully carries a 251-octet packet on the already-required 2M/2M link. Request and accept 251 octets with at least 1590 microseconds, restore the proven 64-chunk unconfirmed-link window, and keep the asynchronous recovery only for a real octet/time reduction below that 2M-safe target. This must stop the repeated 1590-to-2120 LL procedure fight without weakening the 2M/251-byte full-speed proof or changing the confirmed 400 window and 32-total/16-per-lane pipeline. | Static 2M/251/1590 predicate plus 64/400/32 host constants, firmware/Type rebuild and installed/root identities, then consecutive same-root-package runs with no 1590-triggered recovery loop, strict `>60.0 KiB/s`, zero offset recovery and existing reliability gates. |
| LST-MEM-001 | 2026-07-29 | Treat the repeated 1.0.3 internal-SRAM exhaustion as one product-level regression, not a series of isolated allocation failures. Build an explicit v1.0.2-to-current memory ledger covering linked `.data`/`.bss`, static and dynamic task stacks, NimBLE pools, I2S DMA, AFE/VAD, OTA queue/reorder/batch storage and largest-free-block fragmentation. Recover durable internal-memory headroom through ownership/lifetime fixes and eligible PSRAM placement while preserving DMA-capable buffers and the rule that Flash/NVS work never runs on PSRAM task stacks. A normal boot, active BLE link, OTA BEGIN, full transfer, post-OTA audio capture and an interrupted-OTA restore must not report `ESP_ERR_NO_MEM`, task creation failure or `audio_capture_start_failed`; LED6 WARN must not be hidden. | Reproducible v1.0.2/current size and runtime heap ledger, static ownership contracts, firmware build/boot logs, injected-ABORT restoration, and three consecutive full OTA-plus-audio machine passes with internal free/largest-block telemetry and no memory-related errors. |
| LST-MEM-002 | 2026-07-29 | PSRAM placement must follow the ESP-IDF Flash-cache and DMA constraints, because an earlier task/buffer migration caused a device crash. Keep OTA/Flash/NVS task stacks, I2S DMA storage, DMA descriptors, the OTA queue/reorder state and the internal Flash batch in internal RAM. The new 600 ms voice pre-roll PCM history may use PSRAM only as ordinary task-owned data: it is never a DMA target or Flash API input, audio capture is quiesced before OTA Flash writes, allocation failure stops audio initialization cleanly, and both audio capture and AFE fetch task stacks remain internal. Do not move the pre-existing BLE replay window merely to improve the ledger. | Static placement contract, v1.0.2/current map delta showing the 19,200-byte pre-roll removed from internal `.bss`, firmware build, normal boot/audio evidence, interrupted-OTA restoration, and full OTA traces with no cache-disabled panic, stack failure or `ESP_ERR_NO_MEM`. |
| LST-WAKE-002 | 2026-07-29 | A recording capsule must correspond to a legitimate user recording start, not a hidden automatic wake candidate created during OTA preparation or transfer. In addition to the OTA exclusion in LST-OTA-010, unexplained normal-idle capsule appearances must be diagnosed from the activation/session trace and fixed at the trigger source rather than hidden in Type UI. | OTA handoff trace with zero automatic recording/capsule events, plus a normal-idle observation trace correlating every recording capsule to an intentional trigger. |
| LST-WAKE-003 | 2026-07-30 | Idle voice activation must combine high recall with false-wake control. Keep the sensitive streaming KWS as stage 1, then use a bounded local full-phrase confirmation and enrolled-owner verification as stage 2; do not regain precision by switching the primary detector to strict mode or globally raising its threshold. The idle activation phrase and any bounded pre-wake residue must not appear in that automatic session's first capsule, preview, final transcript or insertion, but the same phrase spoken after recording has started remains ordinary dictated speech under LST-WAKE-001. Audio cutting and text guarding must be session-scoped and use an explicit automatic-wake boundary; they must not remove a later in-session occurrence. Preserve the unenrolled phrase-only open gate and keep wake-to-capsule P95 at or below 1200 ms with a 1500 ms ceiling when the local helper is ready. | Focused positive, near-phrase negative, ambient-speech and repeated-in-session phrase tests; saved-candidate replay proving recall and false-wake decisions; automatic-session first-preview/final boundary tests proving only activation residue is removed; installed-Type trace reporting stage-1/stage-2 decision and wake-to-capsule latency. |
| LST-WAKE-004 | 2026-07-30 | Refine the second-chance path without trading sensitivity for false wakes. When the sensitive streaming KWS has not fired, local ASR may recover only a complete activation phrase at the start of the candidate (`ExactStart` or bounded phonetic equivalent); a phrase found later in ordinary ambient speech must not independently open a capsule. Begin that local second chance at about 1000 ms and retry with bounded new-audio steps so a KWS miss can still meet the LST-WAKE-003 latency ceiling. A local-only acceptance must carry a nonzero, original-audio activation endpoint recovered by full-buffer KWS where possible, with a bounded phrase-duration estimate only for a start-aligned full-phrase result when the model exposes no timestamps. Do not change the primary KWS score/threshold, do not switch it to strict mode, and do not remove later in-session occurrences. | Focused fusion tests for local-only start versus later-phrase rejection, endpoint unit tests, saved real-candidate replay including a streaming-KWS miss, near-phrase/ambient negatives, and installed-Type traces showing decision source, nonzero wake endpoint, preserved body text, no unintended capsule and wake-to-capsule at most 1500 ms. |
| LST-WAKE-005 | 2026-07-30 | A keyword-model fallback may bypass a local verifier timeout or unavailability only before that verifier has returned any explicit `Absent` result for the candidate. Once an explicit `Absent` exists, a later timeout, helper error or task failure must keep the candidate in its bounded confirmation path or reject it; it must never reverse that evidence into keyword-only acceptance. Preserve the sensitive KWS score and threshold, the 900 ms secondary budget, the 400 ms retry schedule, the unenrolled open gate and the 1200/1500 ms latency contract. Do not add a fixed raw-volume gate because retained quiet accepted samples overlap the incident candidate. Every capsule must retain a logged activation origin; `VoiceActivation` without a matching physical-input event is a wake-decision event, not evidence of swapped wiring. | Shared decision tests covering zero/one/multiple `Absent` results and verifier timeout/unavailability/failure; Type regression reproducing session 150 ordering; saved-candidate review for acoustically ambiguous cases; full Type and Platform machine gates. |
| LST-WAKE-006 | 2026-07-30 | Remove the intermittent wake tail without weakening recall or false-wake control. The product-sensitive live `StreamingDetector::new` runtime must remain warm when strict in-session detection or offline recall configurations run; auxiliary configurations must not evict the live runtime or accidentally reuse a runtime built with different prefix-variant behavior. Keep the accepted live score/threshold, stage-2 `Absent` authority, local-only `PresentLater` rejection and owner gate unchanged. Report both candidate-start-to-capsule and detected-phrase-end-to-capsule latency so a deliberately slow activation phrase is not mislabeled as system delay. With the helper ready and the live runtime warm, phrase-tail-to-capsule P95 must be at most 350 ms with a 500 ms ceiling while the existing ordinary-pace 1200/1500 ms start-to-capsule contract remains protected. | Cache-key/unit contracts covering live versus auxiliary runtime ownership and prefix-variant identity; saved-candidate positive/near-phrase replay; installed-Type traces showing no repeated live model reload tail, both latency metrics, preserved sensitivity/false-wake decisions and the stated P95/ceiling gates. |
| LST-WAKE-007 | 2026-07-30 | Eliminate artificial wake latency caused by abandoned or terminal candidate work. A completed or discarded voice candidate must not leave blocking helper requests queued ahead of a later real KWS hit: local confirmation is single-flight and non-queueing, a busy helper returns immediately, and the current candidate retries through the existing bounded schedule. Busy is transient backpressure, not verifier unavailability, so it must neither trigger an immediate keyword-only fail-open nor erase explicit `Absent` authority. A terminal ambient candidate with at least two completed local `Absent` results must skip the multi-second offline recall cascade; when that evidence is unavailable, preserve offline recovery but cap its serialized actor wait at 500 ms before releasing the next BLE input. Preserve the accepted sensitive KWS score/threshold, local-only `PresentLater` rejection, owner gate, 400 ms retry cadence and 900 ms final safety budget. With the helper ready, stale ambient candidates must not push a real activation beyond the LST-WAKE-006 phrase-tail target of 350 ms P95 and 500 ms ceiling. | Helper single-flight/non-queueing unit and source contracts; coordinator regressions proving busy retries without keyword-only acceptance, explicit `Absent` remains authoritative, repeated ambient `Absent` skips terminal offline work and otherwise the terminal actor wait is bounded; retained session-84 reproduction or equivalent ambient-backlog-to-real-wake installed trace meeting the phrase-tail latency gate. |
| LST-WAKE-008 | 2026-07-30 | Reject the separate prewarmed KWS-priority helper lane and candidate-handle preemption experiment. Installed session 212 showed that it converted a valid activation into repeated short-snapshot `Absent` results and delayed acceptance until terminal confirmation. Keep the accepted single-helper verification semantics while retaining LST-WAKE-007 non-queueing `try_lock` and bounded terminal cleanup. Future latency work must not discard an in-flight exploratory result that can provide the full phrase, must preserve two-`Absent` authority and sensitivity, and must pass the installed ambient-to-wake machine loop before packaging. | Session-212 rejection trace, source contract excluding a second priority helper lane, full Type regression suite, release gate, and installed positive wake review. |
| LST-WAKE-009 | 2026-07-30 | A late KWS hit must not make stage-2 inference scale with the entire ambient candidate. Once streaming KWS has located the phrase, confirm only the most recent aligned 5,000 ms PCM window ending at the current candidate tail; keep the KWS boundary absolute for transcript offset and latency reporting. Retain the single helper, non-queueing busy retry, explicit-`Absent` authority, local-only `PresentLater` rule, 900 ms safety budget and all accepted sensitivity/false-wake thresholds. Do not restore the rejected priority lane or preempt an in-flight exploratory result. | Source/unit contract for an unchanged short candidate and an aligned 5,000 ms tail from a long candidate; retained installed late-wake replay; full Type tests and release gate; installed late-wake trace meeting the 350 ms P95 and 500 ms phrase-tail ceiling without a keyword timeout fail-open. |
| LST-WAKE-010 | 2026-07-30 | Owner release decision: ship the current Listener 1.0.3 Type and firmware packages with the LST-WAKE-009 latency correction. Do not perform further wake sensitivity, threshold, model or false-wake tuning in 1.0.3. The remaining fixed positive/near-phrase/ambient accuracy matrix and any resulting tuning are deferred to 1.0.4. This is acceptance of the current 1.0.3 release boundary with a known follow-up, not evidence that overall wake accuracy has already passed that future matrix. | Final 1.0.3 package identities, clean tagged Type/Firmware commits, installed session-264 latency evidence, owner deferral note, and a 1.0.4 follow-up retaining all accepted 1.0.3 OTA/BLE/LED behavior. |
| LST-WAKE-011 | 2026-07-31 | Listener 1.0.4 must replace the current poor wake experience with one measured end-to-end wake path that remains usable for soft, normal, loud, fast and far-field speech, does not introduce digital clipping while leveling different speech amplitudes, retains at least 1000 ms of actual pre-roll at the automatic-wake boundary, and can wake repeatedly after connected/disconnected low-power transitions. Use one explicitly owned adaptive gain/limiter policy rather than stacked independent fixed gains; upstream physical clipping must be detected and reported because downstream processing cannot repair it. The fixed positive replay matrix must achieve at least 95% overall recall and at least 90% in every condition, the near-phrase/ambient negative matrix must produce zero capsules and long-form replay no more than one false wake per 12 hours, processed voiced RMS spread across the 24 dB input-level fixture must be at most 6 dB with zero newly clipped samples and peak at most -3 dBFS, and all accepted LST-WAKE-001/003/005/006/007/008/009 plus 1.0.3 OTA/BLE/LED behavior remain protected. | Versioned positive/negative WAV manifest and deterministic replay report; pre/post audio-level and clip telemetry; unit/source contracts for one gain owner, limiter, actual pre-roll and VAD/low-power re-arm; ten-cycle idle/wake simulation plus latest installed Type and flashed firmware hardware trace; phrase-tail P95 <=350 ms / ceiling <=500 ms and candidate-start P95 <=1200 ms / ceiling <=1500 ms; final package identity, full recording path and protected OTA regression evidence before Chinese operator acceptance. |
| LST-AUDIO-001 | 2026-07-31 | Treat Listener 1.0.4 as a focused recording-function refinement release, not a narrow threshold patch. The user-facing bar is a XiaoAi-class casual wake experience together with consistently usable recorded sound: wake, capture, level control, noise handling, clipping protection, BLE PCM integrity and downstream recognition form one end-to-end product path. Do not declare the release ready from KWS replay alone; the installed-Type and flashed-firmware path must turn the accepted wake into non-empty, non-clipped, intelligible audio and return cleanly to idle. This entry clarifies the product scope of LST-WAKE-011 and does not lower any of its numeric gates. | LST-WAKE-011 machine report plus pre/post PCM audio-quality metrics, installed end-to-end wake-to-ASR trace, ten-cycle repeatability evidence and final Chinese operator listening/wake review. |
| LST-AUDIO-002 | 2026-07-31 | During recording, the sound-reactive recording light must remain a truthful meter of the microphone's physical input level: soft, normal and loud speech must produce visibly different intensity instead of all saturating at maximum. Audio preprocessing may independently apply the single owned AGC, noise suppression and final limiter required by LST-WAKE-011, but its levelled output must not feed back into the visual meter. Derive the light envelope from a pre-AGC, pre-limiter raw-input level tap, clamp only for display safety, and keep recording state/color/LED-zone brightness caps unchanged. | Static signal-ownership contract proving the LED meter reads only the raw-input tap; deterministic 24 dB PCM sweep showing monotonic non-saturated meter output with at least three distinct levels while processed voiced RMS remains within its existing <=6 dB spread and clipping gates; flashed-device soft/normal/loud trace plus final owner visual confirmation. |
| LST-AUDIO-003 | 2026-07-31 | The Type recording capsule must show the same truthful microphone-input dynamics as the accepted firmware recording light. Firmware must attach a bounded 0-100 pre-AGC, pre-limiter raw-input level envelope to each VKA1 audio packet using a backward-compatible metadata extension; Type must use that metadata only for capsule visualization and must never feed it into PCM, KWS, ASR, AGC, noise suppression or limiter decisions. Processed BLE PCM remains the unchanged recognition payload. When metadata is absent because an older firmware is connected, Type may fall back to its existing PCM-derived visual level. Soft, normal and loud speech must remain visibly distinct instead of all appearing at maximum, with no audio packet loss, payload corruption or transport-throughput regression. | Firmware ownership/static test proving the transported meter originates before AGC/limiter; protocol tests proving extended and legacy packets preserve byte-identical PCM payloads and decode optional level metadata; Type tests proving metadata overrides processed-PCM display level while legacy packets retain fallback behavior; deterministic 24 dB sweep with at least three distinct non-saturated capsule levels; rebuilt/flashed firmware and installed Type end-to-end trace followed by final owner visual confirmation. |
| LST-AUDIO-004 | 2026-07-31 | The owner accepted the final installed 1.0.4 Type capsule review after softly, normally and loudly repeating the same sentence. Preserve the accepted visibly distinct low/mid/high capsule response, the pre-AGC/pre-limiter firmware metadata source, the unchanged processed PCM recognition path, the legacy firmware fallback and the LST-WAKE-015 wake sensitivity. A later change that can plausibly affect this path must rerun the focused machine contract and Chinese visual review. | Canonical operator result `通过`, adjacent machine summary with deterministic capsule levels 1/21/99, installed 294/294-packet zero-loss metadata trace, final MSI/firmware identities and archived Chinese prompt result. |
| LST-AUDIO-005 | 2026-07-31 | Continue the worthwhile audio refinement after 1.0.4 with three coupled safeguards: (1) produce paired pre-AGC/post-AGC frame statistics for the same captured speech, including voiced input/output level percentiles, effective-gain percentiles, noise-floor estimate, time at the gain ceiling, limiter duty and clipping; (2) bound excessive adaptive gain from the measured pre-AGC noise floor so non-speech does not raise room noise or breathe; and (3) hold the accepted speech gain through voiced spans while applying bounded attack/release transitions instead of abrupt frame-to-frame gain changes. The policy may reduce only gain unsupported by measured signal-to-noise evidence; it must not replace the accepted sensitive KWS/VAD policy, alter the pre-AGC raw meter, add a second Type-side AGC, or reduce the LST-WAKE-011 positive recall/latency gates. The final processed PCM retains zero new clipping and peak at most -3 dBFS. | Shared deterministic gain-policy tests; versioned paired pre/post report over soft/normal/loud/far/noise fixtures; source contract proving one firmware gain owner and an unchanged raw meter; current-tree firmware build; positive/negative wake replay; flashed-device recording with pre/post/gain/noise telemetry, zero transport loss and no WDT regression; final Chinese listening review only after the machine gates pass. |
| LST-WAKE-012 | 2026-07-31 | The 1.0.4 voice-wake guarantee applies only while the device is non-idle and the Bluetooth status light remains on. Once the normal power policy enters the Bluetooth-light-off idle state, microphone/VAD/KWS shutdown and inability to wake by voice are expected product behavior; wake from that state continues to use the existing physical or connection recovery path. This owner decision supersedes only the post-idle voice-rewake language and ten-cycle idle-to-voice-wake gate in LST-WAKE-011 and LST-AUDIO-001. It does not weaken non-idle repeated wake, automatic-session uniqueness, the 1000 ms actual pre-roll requirement, audio-level/clipping gates, or existing idle entry and LED behavior. | Static power-state contract proving voice monitoring is enabled only in the non-idle Bluetooth-light-on states; virtual-clock and real-device evidence that normal idle entry still extinguishes the Bluetooth light and may stop capture; at least ten consecutive non-idle wake-to-stop cycles, with exactly one VoiceActivation session per accepted phrase and no button/restart/re-pair between cycles. |
| LST-WAKE-013 | 2026-07-31 | The final 1.0.4 owner review failed while non-idle in the presence of construction noise. Non-stationary background noise must not raise an auxiliary amplitude/SNR gate until valid owner speech can no longer reach KWS. VADNet is the speech-candidate authority; post-NS level and noise-floor measurements are diagnostic only and must never veto a VADNet speech frame or learn a rejected speech frame as noise. A hidden candidate rejected by Type must re-arm without a blind cooldown, while remaining invisible, bounded, excluded from user-activity timers and unable to prevent Bluetooth-light-off idle. Do not recover recall by weakening the semantic KWS/full-phrase/explicit-`Absent` gates that protect against false capsules. | Deterministic rising and impulsive non-stationary-noise model proving VADNet speech is never amplitude-vetoed and speech frames never contaminate the noise estimate; static hidden-reject immediate-rearm and no-activity/no-sleep-blocker contracts; unchanged fixed negative matrix and 12-hour zero-false-capsule soak; rebuilt/flashed firmware plus installed-Type trace and repeated real non-idle owner wake review in the construction-noise environment. |
| LST-WAKE-014 | 2026-07-31 | The owner rejected the final installed 1.0.4 review as less sensitive than the preceding build. A complete stage-1 KWS hit must not inherit the old 900 ms secondary-confirmation wait when the local helper has an intermittent slow inference: that budget contradicts the accepted 350 ms phrase-tail target and produced a real 1091 ms local confirmation / 1004 ms phrase-tail delay. Keep the existing single helper, full-phrase local confirmation, explicit-`Absent` authority, owner gate and sensitive KWS threshold unchanged, but reduce only the existing no-`Absent` keyword fail-open deadline to 250 ms after the KWS hit. Any completed explicit `Absent` still blocks keyword-only acceptance. This entry replaces only the 900 ms final safety-budget clauses in LST-WAKE-007 and LST-WAKE-009. | Focused cascade tests proving a KWS hit with no explicit `Absent` fails open after 250 ms while one explicit `Absent` still blocks it; unchanged fixed negative matrix and 12-hour zero-false-capsule soak; saved-candidate positives; installed-Type trace with phrase-tail P95 <=350 ms / ceiling <=500 ms followed by owner review. |
| LST-WAKE-015 | 2026-07-31 | The owner accepts the current installed 1.0.4 non-idle wake sensitivity after the LST-WAKE-014 250 ms confirmation-budget change and explicitly reports that it is now good. Treat that sensitivity as a protected regression contract while fixing the Type capsule meter: do not change the KWS threshold, detector mode, local-helper timing, semantic phrase gate, explicit-`Absent` authority, voiceprint gate or firmware VAD/noise policy as part of LST-AUDIO-003. | Focused source and regression evidence showing the capsule-meter change has no wake-policy diff, the accepted wake tests remain green, and a post-install non-idle wake smoke trace remains within the existing LST-WAKE-006/LST-WAKE-014 latency gates. |
| LST-WAKE-016 | 2026-07-31 | The Type wake-phrase setting is a real product control and must not be limited to `开始录音`. A complete validated Chinese phrase selected in Type is the single source of truth for live KWS, local full-phrase confirmation, activation-prefix removal and voiceprint enrollment. Editing must not persist or activate intermediate keystrokes: validate and prewarm the complete phrase first, then switch atomically. Following the XiaoAI-style public product boundary, a voiceprint is bound to the phrase spoken during enrollment; changing the configured phrase invalidates the old voiceprint and requires the owner to record the new phrase three times before `仅本人语音自动开始` is active again. An unenrolled or invalidated voiceprint retains the existing explicit open-gate contract, so a valid phrase can still wake for any speaker; the UI must state that owner-only protection is off until re-enrollment. Preserve the accepted LST-WAKE-015 default-phrase sensitivity and all explicit-`Absent`, local confirmation, idle and recording contracts. | Official Xiaomi/XiaoAI source record; backend phrase normalization/validation and phrase-keyed runtime tests; UI commit-not-keystroke test; stored voiceprint template contains the enrolled phrase and cannot gate a different phrase; phrase-change test proves old voiceprint becomes inactive while no-template open-gate remains; representative custom-phrase positive/negative KWS matrix; rebuilt and installed Type with unchanged firmware, followed by owner changing the phrase, recording it three times and passing a Chinese wake/voiceprint review. |
| LST-WAKE-017 | 2026-07-31 | The owner accepted the installed 1.0.4 LST-WAKE-016 review with no note. Preserve the accepted custom-phrase state machine as a regression contract: no voiceprint means any speaker may wake with the configured phrase; a phrase-bound enrolled voiceprint means only the owner may wake; changing the phrase immediately disables the old voiceprint and requires three new recordings. Do not regress the accepted non-idle sensitivity or change the Bluetooth-light-off idle boundary. | Canonical Chinese operator result `通过`; adjacent installed-package identities, Program Files UI screenshot, 927-test single-thread release suite, two-phrase/two-speaker positive KWS matrix, mismatched-phrase negatives and archived operator result under `Listener-Type/.artifacts/listener-1.0.4-custom-wake-voiceprint/`. |
| LST-WAKE-018 | 2026-07-31 | The installed 1.0.4 path demonstrably auto-started recording for the configured phrase `开始录音` after the owner instead said `现在能换吗` and `开始阅读`; these are final false wakes, not harmless firmware VAD candidates. Reject semantically different and confusable near phrases at the secondary confirmation boundary without changing the protected primary `StreamingDetector::new` threshold/mode, firmware VAD/noise policy, custom-phrase state machine or the 250 ms secondary budget. A completed secondary `Absent` result that is already available inside the budget must prevent keyword-model fallback; timeout/unavailable may retain the existing fail-open only when no explicit contradictory evidence was produced. Preserve LST-WAKE-011 recall of at least 95% overall and 90% per soft/normal/loud/fast/far category, plus the accepted LST-WAKE-015/LST-WAKE-017 live sensitivity and latency ceilings. | Archived capsule timeline and SHA256 identities for the two owner-spoken false-wake WAVs; shared decision tests for completed `Absent` versus timeout/unavailable; deterministic real-WAV negative replay with zero activation; unchanged positive matrix gates; freshly installed Type identity and repeated live positive/negative review without a firmware or wake-threshold change. |
| LST-DIAG-001 | 2026-07-30 | Default live wake-diagnostic WAV capture is a privacy-bounded rolling store across Type restarts, not a per-process counter. Under `%LOCALAPPDATA%\Listener Type\Logs\wake-diag-live`, retain at most 128 matching WAV files, at most 32 MiB total and at most 7 days of age; remove oldest matching files first and never delete unrelated files. Apply cleanup before the first capture in each process and periodically thereafter so repeated restarts cannot grow the store without bound. An explicit `LISTENER_WAKE_DIAGNOSTIC_DIR` remains operator-managed and must not be pruned implicitly. Diagnostic cleanup must not run on the BLE/KWS compute path in a way that delays capsule release. | Pure retention-plan tests for age/count/bytes/unrelated files, filesystem integration evidence across simulated restarts, installed-Type directory count/size proof after first live capture, and wake-tail latency evidence showing no cleanup regression. |
| LST-PAIR-001 | 2026-07-29 | Fresh Windows pairing after an explicit Listener recovery action must retain the accepted `v1.0.2` no-I/O Just Works behavior: selecting the current `Blistener` Swift Pair/system prompt during the active recovery window must create a firmware bond and a usable Windows HID/GATT device, not end in a generic pairing-failed popup. Type must not race an in-progress native pairing prompt with background PairAsync, repeated Bluetooth Settings launches, or an adapter restart. | Live firmware SMP/GAP serial trace and installed-Type log from recovery-window open through Windows pairing, followed by bonded-peer proof, notify-ready proof, and one embedded audio session; focused concurrency tests proving only one pairing owner acts at a time. |
| LST-PAIR-002 | 2026-07-30 | Fix EC11 double-click recovery at the pairing-ownership boundary, not with longer GATT retries, fixed sleeps, popup suppression, or a manual Windows Settings workaround. Once Type observes the pre-authorized recovery disconnect and matching advertisement, local stale-pair cleanup, any exact-cache fallback, and the bounded no-I/O `PairAsync` sequence are one atomic Type-owned Windows pairing transaction. A delayed post-`TYPE:READY` ghost prune or other background pairing maintenance must not interleave with that transaction; if maintenance was already in flight, the recovery path waits for ownership and then continues pairing arbitration directly instead of reopening the stale address for a full GATT timeout ladder. | Focused ownership/order test proving one maintenance guard spans cleanup through final PairAsync and that an in-flight maintenance owner is awaited without a stale-GATT detour; current MSI identity; at least three randomized installed-Type generated EC11 double-click runs with trigger-to-`TYPE:READY <= 10000 ms`, fresh-pairing-to-`TYPE:READY <= 6000 ms`, followed by an embedded audio session. |
| LST-LAT-001 | 2026-07-29 | Fix any 1.0.3 latency regression relative to 1.0.2 without reverting OTA throughput. Preserve the accepted warm startup reconnect target (at most 1000 ms) and the existing recording/session latency gates. | Startup reconnect check, focused recording/session tests, and post-OTA recording evidence from the installed build. |
| LST-WAKE-001 | 2026-07-29 | The wake phrase is a control trigger only while Listener is idle and waiting to start a recording. Once a dictation/recording session has started, saying the wake phrase again inside a normal sentence must remain ordinary dictated speech: Type must not consume, trim, suppress, or reinterpret that audio/text as another wake command. Preserve the existing idle wake behavior and unenrolled open-gate behavior. | Focused session/KWS tests proving idle wake still starts a session while an in-session wake-phrase occurrence reaches the ASR/transcript path unchanged. |
| LST-REL-001 | 2026-07-30 | The owner manually accepted the final 1.0.3 behavior and requested repository cleanup plus a 1.0.3 release tag. The tagged Firmware and Type revisions, the root `ListenerFirmware_1.0.3_ota.zip` and `ListenerType_1.0.3_x64_en-US.msi`, and the installed Type payload must be reproducible from the same committed source state. Preserve the accepted dual-lane 400/64/32 OTA path and its strict `bulk_kb_s > 60.0` evidence; release cleanup must not replace it with the earlier slower 1.0.2 transport. | Owner run with full `1,389,776/1,389,776` transfer, `bulk_kb_s=60.5`, zero offset recovery, one old-to-new device generation boundary and final response-bearing `TYPE:READY`; clean committed worktrees, rebuilt package hashes/manifest identities, release checks and verified local tag targets. |
| LST-REL-002 | 2026-07-30 | The owner accepted the installed explicit-absence wake fallback refinement as looking acceptable and more sensitive than before, then requested that platform extraction stop if the shared boundary is already sufficient and that 1.0.3 be finalized. Stop at the PLAT-REQ-006 pure-decision boundary: keep Windows BLE operations, KWS/ASR engines and models, phrase/product thresholds, PCM ownership, firmware/LED behavior and UI/log sinks in Listener adapters. Finalization must preserve the installed Type MSI identity, the existing accepted Firmware ZIP and OTA throughput path, and must not move the Firmware tag away from the source used by that ZIP merely for documentation-only commits. | Canonical Chinese human review result adjacent to the wake evidence; clean Type/Firmware/Platform worktrees; Type tag resolves to the final Type release record, Firmware tag remains at the packaged firmware source, root artifact and installed payload hashes match the recorded evidence, and both release gates pass. |
| LST-REL-003 | 2026-07-31 | Owner release decision: finalize the accepted coupled Listener 1.0.4 Type, Firmware and shared audio-protocol work as clean local release commits, update the root `ListenerType_1.0.4_x64_en-US.msi`, `ListenerFirmware_1.0.4_ota.zip` and checksum file from those committed source identities, install/flash the exact delivered artifacts, rerun the protected recording/wake/package gates, and create local `v1.0.4` tags at the package source revisions. Keep ignored build, validation and diagnostic artifacts out of Git. Do not discard, stash or revert accepted 1.0.4 work merely to make a worktree appear clean, and do not push commits or tags without a separate explicit owner push request. | Clean Platform, Type and Firmware worktrees; package manifests and SHA256 identities tied to committed source; Program Files executable matches the MSI payload and is running; device serial identity matches the delivered firmware; installed-Type recording path completes with zero packet loss; release checks and root-artifact gate pass; local commit/tag targets and push status recorded. |
| LST-STAB-001 | 2026-07-31 | Listener 1.0.4 must not reboot, reset BLE, or restart the embedded audio session while a non-idle recording or voice-activation candidate is active, including while USB powered and exposed to sustained construction noise. The observed incident is an `ESP_RST_TASK_WDT` root-cause failure after PCM session 94, not a Type-process restart or a normal radio disconnect. Fix the audio hot path itself: every I2S wait must be bounded and recover the running RX channel after a real stall, and continuous VADNet/NS/AGC/limiter processing must reserve a bounded scheduler opportunity for the watched idle task. Do not disable, lengthen, or unmonitor the Task Watchdog to hide starvation. Preserve the accepted LST-WAKE-015/LST-WAKE-017 non-idle sensitivity, custom phrase and phrase-bound voiceprint behavior, AGC/limiter PCM, raw-input recording meter and Bluetooth-light-off idle boundary. | Correlated Type timeline plus retained firmware `DIAG_SYS_BOOT_SAFETY reset_reason=6` / `~BOOT:STATUS task_wdt(6)` incident artifact; static contracts for bounded I2S reads, in-place RX recovery and DSP idle budget with the 5 s watchdog unchanged; current-tree firmware build/package/flash identity; repeated long and rapid-cycle recordings on the flashed device with continuous PCM, no boot marker/session-counter discontinuity, no Task WDT and a final installed-Type user-path recording. |
| LST-STAB-002 | 2026-07-31 | The owner accepted the installed Listener 1.0.4 recording-stability review after the LST-STAB-001 root fix and reported that the device no longer restarted during the reviewed recording. Preserve the bounded 250 ms PDM I2S wait with in-place RX recovery, the bounded CPU1 idle-task scheduler opportunity, and the strict 5 second panic Task Watchdog with both idle tasks watched. A later change to the PDM capture task, VADNet/NS/AGC/limiter scheduling, FreeRTOS task core/priority, BLE audio streaming, or recording session boundaries must rerun the focused static contracts, a continuous recording, repeated start/stop cycles, installed-Type zero-loss evidence, and the Chinese recording-stability review. | Canonical operator result `通过` with note, adjacent machine summary, 150 second session 4 with 11,109/11,109 packets and zero transport/capture failures, ten zero-loss start/stop cycles through session 14, final `reset_reason=usb(11) crash_count=0`, unchanged `task_wdt=1 panic=1 timeout_s=5`, flashed firmware and installed Type identities under `.artifacts/listener-1.0.4-mid-recording-reset-20260731/`. |
| LST-STAB-003 | 2026-07-31 | The owner observed another mid-recording restart after LST-STAB-002 acceptance, and retained firmware state again proves `ESP_RST_TASK_WDT` while Type remained alive. LST-STAB-002 is therefore historical acceptance evidence, not release closure. Before 1.0.4 can ship, remove the remaining ESP-SR scheduling defect: the real-time I2S/feed producer and AFE fetch/DSP consumer must execute concurrently on separate ESP32-S3 cores as in Espressif's AFE task model, rather than co-locating the heavier VADNet/NS/AGC/limiter path with feed until an internal ring operation can stop watchdog progress. Retain bounded I2S recovery and the strict 5 second panic watchdog. Persist the Task WDT offender names, failed-core mask and current per-core task names across reset so any recurrence is attributed rather than inferred. Do not tag or publish 1.0.4 until a post-flash recording soak spans at least the prior roughly 16 minute failure horizon, repeated automatic and manual sessions remain zero-loss, and a fresh owner review passes. | Second incident artifact correlating Type session 25 disconnect with retained `reset_reason=task_wdt(6)` and continuous Type `app_ms`; static cross-core producer/consumer and retained-WDT-attribution contracts; current-tree build/package/flash identity; at least 20 minutes non-idle connected soak including continuous and repeated recordings, no reset/session counter discontinuity, zero Type missing packets, final boot/WDT status, then canonical Chinese review. |
| LST-STAB-004 | 2026-07-31 | Final 1.0.4 release validation found a third retained Task WDT after LST-STAB-003's connected soak: CPU0 was running `IDLE0`, CPU1 was running `audio_afe_fetch`, and the missed subscribers were `health_task` plus `voice_recording_control_task`. This is a recording-state serialization failure, not whole-core scheduler starvation. The recording controller must be a single-writer actor: BLE, USB, keyboard and power callbacks enqueue bounded copied events and never execute recording transitions while holding a shared state mutex; health reads an atomic session snapshot and cannot wait behind recording I/O. Keep event processing bounded and observable, reject queue overflow, and retain the strict 5 second panic watchdog and all accepted wake/audio behavior. LST-STAB-003 remains useful cross-core evidence but is not release closure until the connected and Type-offline noisy paths both pass without a new retained timeout. | Retained attribution showing `IDLE0` plus missed `health_task`/`voice_recording_control_task`; static single-writer/no-unbounded-state-lock contract; focused control burst and power-transition checks; fresh flash followed by at least 20 minutes connected construction-noise candidate cycling and a separate Type-offline non-idle interval, with one uninterrupted boot, no new retained timeout, zero-loss installed-Type recording, final package identity, then canonical Chinese review. |
| LST-STAB-005 | 2026-07-31 | Listener Type 1.0.4 must not exit during install replacement, startup, background-notify refresh, reconnect, OTA handoff, or normal shutdown. The retained Windows WER signature `Windows.Devices.Bluetooth.dll` / `0xc0000005` at offset `0x3ad9a` is a Type-side WinRT BLE lifetime failure, not a firmware reset or ASR error. Event handlers must be detached before their source objects are released; cancellation must stop connection maintenance; and Type must not synchronously `Close()` a `BluetoothLEDevice`, `GattSession`, or `GattDeviceService` while WinRT discovery, GATT operations, or callbacks may still be in flight. Preserve automatic startup reconnect, notify readiness, OTA recovery, pairing, and lossless embedded audio behavior. | Archived WER incident signature and correlated startup log; static contract covering handler-first cleanup and no synchronous WinRT Bluetooth target `Close()` calls; focused Rust/package tests; freshly installed 1.0.4 identity; repeated Program Files `--quit`/launch/refresh cycles reaching `TYPE:READY` without a new matching Application Error/WER event; final installed-Type recording. |
| LST-STAB-006 | 2026-07-31 | The owner reports that the current installed 1.0.4 still drops the Listener link and asks to restore the previously accepted sensitivity. Treat the observed rapid-restart `GattCommunicationStatus(3)` / approximately 10.5 second notify recovery as a failed connection-lifetime regression, not a passing no-crash result. Normal quit, refresh and reconnect must finish releasing the previous notify owner before a replacement process opens GATT; repeated installed warm restarts must reach notify-ready within 3000 ms without `AccessDenied`, pairing, adapter reset or firmware reboot. Preserve the protected LST-WAKE-015/LST-WAKE-017 wake sensitivity exactly: do not change the KWS threshold, detector mode, 250 ms local-helper fail-open budget, semantic/explicit-`Absent`/voiceprint gates, or firmware VAD/noise policy while fixing the link. | Correlated installed-Type log showing the failed 10.5 second recovery; focused shutdown ownership/order tests; source diff proving no protected wake-policy change; freshly installed MSI with at least three consecutive graceful quit/restart cycles below 3000 ms, no new matching WER event, uninterrupted firmware boot identity and one zero-loss recording. |
| LST-OTA-072 | 2026-08-01 | Listener 1.0.4 OTA must keep the accepted strict sustained `bulk_kb_s > 60.0` path after a Type-driven BLE rename and must not exhaust the firmware's internal NimBLE ACL receive pool. Treat the observed `active_link_confirmed=false`, 29.3 KiB/s transfer and repeated `ACL buf alloc failed` messages as one fast-link readiness/backpressure defect, not whole-heap exhaustion and not a reason to hide LED6 WARN. A secure 7.5-15 ms, 2M/2M link reporting `251 octets / 340 us` is not bulk ready. On the release Windows controller, the accepted `ThroughputOptimized` request negotiates `251 octets / 965 us`; firmware must require at least that measured profile before BEGIN. Type must retain the returned request long enough to prime DLE, then close it and wait for the firmware-owned 7.5 ms interval because holding the host preset pins 15 ms and measured only 35.3 KiB/s. The OTA payload must be link-aligned rather than pretending 965 us can carry the old packet efficiently: use a 443-byte firmware chunk so `4 L2CAP + 3 ATT + 4 OTA + 443 data = 454 bytes`, fitting exactly two approximately 227-byte 2M link packets, while the old 500-byte chunk required three. Preserve compatibility with manifests advertising up to the protocol maximum 500 bytes by negotiating the host chunk down to 443. The N16R8 product has 8 MB PSRAM: allocate NimBLE host memory externally and reserve 128 host ACL receive buffers there. Use a bounded 40-total/20-per-lane desktop burst while retaining the device's 32-entry Flash queue; the aligned worst case is 80 ACL fragments, leaving 48 of the 128-buffer pool for control and notification traffic, while the 400-chunk window avoids response-bearing SYNC overhead. The prior 440-byte/32-write shape reached 61.0 KiB/s once but a second run missed the strict 60 KiB/s gate by 29 B/s, so it is evidence of the correct packet shape but not a stable release result. Diagnostics must distinguish internal RAM, PSRAM, largest internal block, DLE octets/time and ACL-pool pressure. Remove the user-facing transfer-speed average label while retaining machine throughput telemetry and the strict release gate. Preserve rename, bonded reconnect, zero-recovery OTA, one expected software reboot, final response-bearing `TYPE:READY`, recording/wake behavior and all accepted LED renderers. | Archived rename/OTA incident trace, the zero-ACL but slow 8-depth experiment, the PSRAM/ACL allocation evidence, the failed 340-us 400-window runs, the retained host request negotiating 251/965 but pinning 15 ms and 35.3 KiB/s, the two-phase 500-byte 251/965 at 7.5 ms run reaching only 54.6 KiB/s, the post-release attempt proving this controller will not negotiate 1064 us, the historical host request that was dropped immediately, the 440/32 aligned runs at 61.0 KiB/s and 29 B/s below the gate, and prior 63.5-68.8 KiB/s evidence; focused PHY-specific DLE readiness/relink, two-phase WinRT DLE-prime/request-release lifetime, 443-byte two-link-packet negotiation, PSRAM-backed 128-ACL/40-host/32-device pipeline, UI-no-average and diagnostics contracts; firmware/Type builds; flashed/installed rename followed by at least three same-image OTAs, each strictly above 60.0 KiB/s with zero ACL allocation failures, zero offset recovery, one software reboot, no WARN/OOM/WDT regression and final `TYPE:READY`; final recording smoke. |
| LST-OTA-073 | 2026-08-01 | Machine closure for LST-OTA-072: accept the 443-byte/40-write host shape for the 1.0.4 candidate. The freshly installed Program Files build transferred the same 1,688,064-byte image in three consecutive runs at 68,108.3, 66,141.5 and 68,185.3 B/s, all strictly above 60 KiB/s, with 3,811 chunks, zero offset recovery, fixed time 6,122/6,144/6,077 ms, no ACL allocation failure, no OOM/assert/Task-WDT reset, one expected software reboot and final `TYPE:READY` each time. Post-run status reports internal free 83 KB, largest internal block 31 KB, DMA free 75 KB, PSRAM free 7,133 KB, connected 2M/2M, 251-byte DLE, 7.5 ms interval, OTA inactive, crash count zero and LED6/WARN off. The silent installed-Type recording smoke completed 127 expected packets and returned to background notify-ready; empty ASR text is expected because no speech stimulus was supplied. Human acceptance remains deferred per owner instruction and is not inferred from machine evidence. | `listener-ota072-aligned443-ui-run1..3.json` and matching `.serial.log` artifacts; `listener-ota072-aligned443-post-audio.json` plus Type timeline; `listener-ota072-aligned443-final-status.serial.log`; installed MSI SHA-256 `f8f65f94dc3b511bb71c52f14b1da475ca792a310e5218ab73cb57c08daf2054`; delivered OTA ZIP SHA-256 `2b425de53a636c28cfd9d8fc4b381d30b92ad785fe39b9029e515bb2ee720cf3`. |
| LST-OTA-074 | 2026-08-01 | Owner correction before republishing 1.0.4: removing the word and behavior of the cumulative `平均` speed must not remove transfer-speed visibility. During the Type OTA `transferring` state, show a compact live `x.x KiB/s` value next to byte progress, derived from the latest bounded progress segment rather than the whole-transfer average. Do not label it `平均`, do not use it as the release acceptance calculation, and keep the strict machine gate based on complete protocol transfer time. Hide/reset the value outside active transfer. | Focused speed-estimator tests proving recent-segment rather than first-to-last behavior; production frontend build; installed Program Files screenshot or DOM evidence during the final same-package OTA showing a visible `KiB/s` value with no `平均` label; existing strict `>60 KiB/s` machine result remains independently PASS. |
| LST-OTA-075 | 2026-08-01 | The first exact-root stable 1.0.4 republish attempt exposed a start-of-bulk link-settling race: WinRT reported the firmware-owned 7.5 ms interval once, but the transfer then took 49,696 ms at 33.2 KiB/s and recovered one 127-chunk offset gap. Supersede the one-sample handoff proof and the unconditional accepted-BEGIN link request from LST-OTA-060. After releasing `ThroughputOptimized`, Type must observe the 7.5 ms interval continuously for a bounded 250 ms settle period before BEGIN; any slower sample resets that proof. Because the BEGIN hard gate already proves secure 7.5-15 ms, 2M/2M and 251-octet/965-us readiness, an accepted BEGIN must preserve that established link and must not launch a redundant PHY/DLE/connection-parameter procedure into the first data window. Keep this wait outside the full-protocol transfer denominator but inside the existing `<7000 ms` fixed-time release gate; do not change the 443-byte chunk, 40-write pipeline, 400-chunk window, speed calculation or strict `>60 KiB/s` threshold. | Failed exact-root artifact `listener-104-republish-final-ota.json` and matching serial/Type logs; focused Type continuous-interval and firmware no-redundant-BEGIN contracts; rebuilt stable MSI/ZIP identities; then at least three consecutive exact-root installed-Type OTAs strictly above 60.0 KiB/s with 3,811 chunks, zero offset recovery, one software reboot, fixed time below 7000 ms, no ACL/OOM/WDT/assert/WARN regression and final `TYPE:READY`, followed by the installed recording smoke. |
| LST-OTA-076 | 2026-08-01 | Reject the LST-OTA-075 settle/no-BEGIN-request experiment. Its exact-root run continuously observed 7.5 ms before BEGIN yet regressed to 17.1 KiB/s, 3,856 writes, one offset recovery and 9,292 ms fixed time; the post-run device was still 2M/2M at 7.5 ms but had fallen from the accepted `251 octets / 965 us` DLE profile to `251 / 340 us` with recovery pending. The connection interval is therefore not an airtime proof, and releasing the WinRT firmware-update request before bulk lets Windows withdraw the negotiated packet-time budget. Follow Microsoft's `RequestPreferredConnectionParameters` firmware-update lifetime contract with the now link-aligned 443-byte payload: retain the exact bulk target's successful `ThroughputOptimized` request through `denzic_ota_core::transfer`, close it on every target success/error/drop, and let the BEGIN path keep its bounded active-link refresh. This reevaluates the old retained-lease result from LST-OTA-062 because that run used the pre-alignment packet shape; it does not lower the strict speed gate or change chunk, pipeline, window, queue, timeout or throughput accounting. | Failed artifact `listener-104-republish-stable-run1.json`, post-status serial proof of `dle_tx_time_us=340`, official Microsoft request-lifetime documentation, focused request-ownership/drop and immediate-BEGIN contracts, then current-build A/B followed by three exact-root installed-Type OTAs strictly above 60.0 KiB/s with 3,811 writes, zero recovery, one reboot, `<7000 ms` fixed time, no ACL/OOM/WDT/assert/WARN and final `TYPE:READY`; final recording smoke. |
| LST-OTA-077 | 2026-08-01 | Refine LST-OTA-076 from full-transfer retention to an air-confirmed first-window handoff. The aligned current-build A/B retained `ThroughputOptimized` for the full transfer and eliminated recovery (3,811 writes, zero offset recovery, 6,004 ms fixed time), but Windows held the link at 15 ms and throughput was only 45.3 KiB/s. Retain the request through BEGIN and the first 400-chunk bulk window; release it only after the first response-bearing SYNC has completed, which proves the prior WWR queue crossed the air/link and reached firmware rather than merely completing WinRT handles. Firmware then owns the return to 7.5 ms while the already-established 251/965 DLE profile is active. Closing must be idempotent and still occur on every earlier error/drop path. Do not replace this proof with a sleep, exclude the first window from throughput timing, or alter the 443-byte chunk, 40-write pipeline, 400-chunk window, strict `>60 KiB/s` gate or `<7000 ms` fixed-time gate. | A/B artifact `listener-104-retained-lease-ab-run1.json` and Type timing log; focused first-SYNC ownership/idempotent-close contract; current release build A/B, then three exact-root installed-Type passes with the existing speed, recovery, reboot, reconnect, memory and recording gates. |
| LST-OTA-078 | 2026-08-01 | Complete the LST-OTA-077 handoff at the first SYNC's paired uncached status read, not at the SYNC write response alone. The first experiment reached 59.2 KiB/s with zero recovery, but one reconnect exceeded the fixed gate; the repeat released immediately after SYNC, then the adjacent status read failed four times, causing one 95-write recovery, 23 status reads and 56.1 KiB/s. A SYNC write response proves ordered delivery, while its successful uncached status read is the protocol's authoritative offset and active-link proof. Close `ThroughputOptimized` idempotently only after that read succeeds with `control_sequence == 2`, so the Windows parameter transition cannot invalidate the confirming read. Preserve all LST-OTA-077 accounting and performance gates. | Artifacts `listener-104-first-window-handoff-ab-run1.json` and `...run2.json` plus Type control/status timing; focused close-after-successful-status ordering contract, then the existing current-build and final installed exact-root repeat gates. |
| LST-OTA-079 | 2026-08-01 | Bound the LST-OTA-078 air-confirmation window to 200 chunks. Closing after the successful first status eliminated recovery (3,811 writes, 11 status reads, 6,135 ms fixed time), but a 400-chunk first window spent about four seconds under Windows' 15 ms request and the complete protocol measured 57.0 KiB/s. A 200-chunk window is 88,600 payload bytes, five times the 40-write instantaneous host pipeline and safely below the 10-second progress watchdog even at the measured retained-request rate. Use 200 as the complete transfer window so every extra SYNC/status cost remains inside the strict denominator; release the request only after the first paired status succeeds, then run the remaining windows on the restored 7.5 ms link. Keep 443-byte chunks, 40-write pipeline, 32-entry device queue and all reliability thresholds. | Artifact `listener-104-status-confirmed-handoff-ab-run1.json` and per-window timing; focused 200/40/443 contract; current release A/B followed by three exact-root installed-Type passes above 60.0 KiB/s with zero recovery and all existing fixed/reconnect/memory/recording gates. |
| LST-OTA-080 | 2026-08-01 | Supersede the whole-transfer 200-chunk window from LST-OTA-079 with a first-window-only air-confirmation bound. The measured all-200 run was reliable but still only 57.9 KiB/s because its extra SYNC/status rounds remained in the strict denominator. Send the first 100 chunks under the retained WinRT `ThroughputOptimized` request, release that request only after the paired uncached status succeeds, and restore the negotiated 400-chunk window for every subsequent active-link round. The first window still carries 44,300 payload bytes, exceeds the 40-write host pipeline, and proves on-air progress without spending a full slow-link window. Preserve 443-byte chunks, the 40-write pipeline, firmware-owned 7.5 ms recovery, current live `KiB/s` UI without an `平均` label, and every existing reliability and performance threshold. | Artifact `listener-104-window200-handoff-ab-run1.json`; focused shared-core test proving first-window 100 then configured-window 400; production Type build; current-build A/B followed by three consecutive exact-root installed-Type passes strictly above 60.0 KiB/s, 3,811 writes, zero offset recovery, one reboot, fixed time below 7000 ms, no ACL/OOM/WDT/assert/WARN and final `TYPE:READY`; DOM or screenshot evidence of the live current-speed value; final recording smoke. |
| LST-OTA-081 | 2026-08-01 | Tighten the LST-OTA-080 first-window-only bound from 100 to 64 chunks. The first current-build run transferred exactly 3,811 chunks with no protocol error and 5,920 ms fixed time, but 1,688,032 bytes in 27,565 ms was 61,238 B/s, narrowly below the strict 61,440 B/s gate. A 64-chunk first window still exceeds the 40-write host pipeline and its paired SYNC/status remains an authoritative on-air proof, while shortening the retained 15 ms phase before restoring 400-chunk active-link windows. Treat a non-mounted firmware panel as no UI evidence: machine collection must only accept the dedicated OTA speed element, never unrelated page text containing `平均`. Preserve all prior reliability, display and release thresholds. | Failed artifact `listener-104-first100-window400-ab-run1.json`; focused 64-to-400 shared-core and Type contracts; repeat current-build A/B, then final installed exact-root repeat gates and dedicated speed-element screenshot or DOM evidence. |
| LST-OTA-082 | 2026-08-01 | Fix the first-window handoff when the firmware `active_link_confirmed` status flag lags the real link. The owner's visible installed-Type transfer correctly showed a recent-segment `36.0 KiB/s`; the matching complete protocol log measured 35.8 KiB/s, 45,985 ms and 30 status reads with `active_link_confirmed=false`, even though the post-run device reported the healthy 7.5 ms, 2M, 251-octet/965-us link. The shared core restored 400 only inside the active-flag branch, so every later successful round fell back to the 64-chunk inactive window. A successful first bounded SYNC/status with the exact expected offset is the authoritative on-air proof: after it, keep the negotiated 400-chunk window even while the advisory active flag lags; an offset mismatch may still halve one recovery window. Keep the UI as current/recent speed rather than an average, retain the 64-chunk first window and every existing reliability, reconnect, memory and strict `>60 KiB/s` release gate. | Owner screenshot; installed Type log at 2026-08-01 06:29 UTC showing 35.8 KiB/s/30 status reads/zero offset recovery; post-run serial `listener-104-visible-slow-post-status.serial.log`; focused shared-core inactive-flag test proving SYNC windows 64 then 400; rebuilt/installed MSI; then visible installed-Type and headless exact-root repeats strictly above 60 KiB/s with the existing gates and recording smoke. |
| LST-OTA-083 | 2026-08-01 | Supersede LST-OTA-082's forced 400-chunk interpretation. The experiment bypassed a genuinely incomplete link and regressed to about 19.2 KiB/s with three offset recoveries. Synchronized Type and firmware traces then identified the actual lifecycle defect: the prepare target and secure bulk target each created a separate WinRT `ThroughputOptimized` request. The first delayed Windows policy update lowered DLE to 251/340 and firmware successfully restored 251/965; the second delayed policy update lowered it to 251/340 again, after which repeated firmware requests produced no completing data-length event. Preserve one successful throughput request continuously across the prepare-to-secure-target handoff, prevent the secure reopen from issuing a second request, and close that same request only after the existing first-window paired status proof. Firmware `active_link_confirmed` remains authoritative; do not send a 400-chunk window while DLE readiness is false. Preserve the recent-segment speed UI, 64-chunk first window, 443-byte chunks, 40-write pipeline, 400-chunk active window, zero recovery, strict `>60 KiB/s`, `<7000 ms` fixed-time, reboot/reconnect/memory/recording and LED contracts. | Failed forced-window artifact `listener-104-active-lag-fix-ab-run1.json`; synchronized slow artifact `listener-104-handoff-rootcause.json` measuring 18.2 KiB/s/40 status reads and `listener-104-handoff-rootcause.serial.log` proving two 251/340 downgrades around one successful 251/965 restoration followed by repeated incomplete data-length requests; focused single-request handoff tests; current-build A/B with serial proof of one downgrade/restore; then three installed-Type passes above 60 KiB/s with all existing gates. |
| LST-OTA-084 | 2026-08-01 | Refine LST-OTA-083: a WinRT preferred-parameter request is scoped to the connection target that created it and must not be moved across the prepare-target drop and secure-connection reopen. The moved-request experiment began with firmware reporting a ready 7.5 ms/2M/251/965 link but still measured only 18.4 KiB/s, 3,816 writes and one recovery. OTA discovery and the disposable prepare target must create no preferred-parameter request. Only the freshly reopened secure bulk target may create one `ThroughputOptimized` request, retain it through the existing first 64-chunk paired status, and close it idempotently. A retry may create one new request only after its previous secure target has been dropped. Keep the firmware readiness flag authoritative and preserve every LST-OTA-083 reliability and performance threshold. | Failed moved-request artifact `listener-104-single-request-ab-run1.json` and matching serial log; Type log proving one request was moved from prepare to secure bulk; focused no-request-on-discovery/single-request-per-secure-attempt contract; serial current-build A/B followed by the existing installed repeat gates. |
| LST-OTA-085 | 2026-08-01 | Complete LST-OTA-084 at the firmware link-layer recovery root. The exact secure-target request experiment showed DLE reaching 251/965 and BEGIN succeeding, but a later Windows 251/340 update entered a persistent oscillation: every downgraded DLE event scheduled the generic active-link worker, which redundantly requested an already-active 2M PHY before requesting data length; that successful but unnecessary PHY procedure repeatedly caused another 251/340 event. When the connection interval is active and cached TX/RX PHY are already 2M, firmware must request only the missing data-length layer and must not issue another PHY request. Connection-update completion must use the same layer-aware rule. A real non-2M link must still request 2M first and let PHY completion request DLE. Preserve the strict readiness predicate, retry timeout, 7.5 ms preference/15 ms fallback, all OTA geometry and all existing performance/reliability gates. | Failed artifact `listener-104-bulk-target-request-ab-run1.json` at 15.4 KiB/s/3,977 writes and matching serial trace showing repeated 340/965 oscillation plus redundant successful 2M PHY updates; focused static/layer-state tests, firmware build/flash, serial proof that a 2M DLE downgrade triggers DLE-only restoration without a PHY request, then the existing current and installed repeat gates. |
| LST-OTA-086 | 2026-08-01 | LST-OTA-085 correctly removed redundant PHY procedures but cannot by itself override the Windows central's receive-time policy: after Type closed `ThroughputOptimized`, the central repeatedly returned DLE to 251/340 and ignored bounded DLE-only requests, producing 19.4 KiB/s despite zero recovery. Retain the exact secure target's request until transfer completion so Windows keeps 251/965. At accepted BEGIN, firmware gets exactly one OTA-scoped retry of the preferred 7.5 ms interval even if an earlier pre-BEGIN collision/rejection enabled the 15 ms fallback; clear only the active-preference fallback/backoff state, keep the generic per-connection policy unchanged, and do not loop this forced retry from status reads. If Windows rejects the OTA retry, remain on the stable 15 ms fallback with the full DLE rather than closing the host request and entering 340-us oscillation. Preserve authoritative readiness and all strict release gates; do not claim the `>60 KiB/s` gate passed unless the retained-request plus bounded 7.5 ms combination proves it on this machine. | Failed post-PHY-fix artifact `listener-104-dle-layer-fix-ab-run1.json` at 19.4 KiB/s and serial trace proving DLE-only retries were ignored after request close; focused full-transfer request lifetime and one-shot OTA preferred-interval retry contracts; firmware/Type A/B with serial connection/DLE proof, then existing installed repeats. |
| LST-OTA-087 | 2026-08-01 | Supersede reuse of a parameter-ready pre-OTA connection. The retained-request plus one-shot preferred-interval candidate still measured 17.9 KiB/s with two offset recoveries, proving that 7.5-15 ms/2M/251/965 snapshots do not expose stale Windows GATT queues or historical preferred-parameter operations. A `TYPE:OTA` handoff must always terminate the current physical BLE connection once, after the existing deferred radio-quiet handoff, and Type must reopen the secure OTA service on the new connection before BEGIN. Do not keep the old connection merely because its parameters look fast. Preserve the bond, CCCD/recovery rules, bounded reconnect/open retries, one OTA reboot, no destructive pairing recovery, full-transfer DLE request lifetime, layer-aware firmware recovery and every existing speed/fixed-time/memory gate. | Failed artifact `listener-104-retained-force7p5-ab-run1.json`; Type timing with 4-11 second response-bearing control stalls despite a retained request; focused always-new-connection handoff contract, serial proof of one pre-BEGIN disconnect/new secure connection, then current and installed repeat gates. |
| LST-OTA-088 | 2026-08-01 | Supersede LST-OTA-087's unconditional physical reconnect. The synchronized clean-generation experiment proved that Windows established the new secure connection at 15 ms and rejected both firmware 7.5 ms requests with HCI unacceptable-connection-parameters status 571 while the WinRT `ThroughputOptimized` request remained active. The complete transfer measured only 25.1 KiB/s, 4,001 writes and 65,765 ms. Keep an existing connection when the strict OTA-ready predicate is already true; disconnect only a genuinely low-power/not-ready link. The accepted throughput shape remains a short first-window warm-up followed by steady 60-70 KiB/s segments, with the complete protocol strictly above 60 KiB/s. Do not publish any candidate that merely changes the displayed speed or depends on a transient peak. | Failed artifact `listener-104-clean-generation-ab-run1.json`; serial `listener-104-clean-generation-ab-run1.serial.log` proving one old-link disconnect, one new secure connection, 15 ms interval, two status-571 rejections and 251/965 DLE; compare the three accepted installed artifacts at 64.0-65.9 KiB/s complete and 63-72 KiB/s steady segments. |
| LST-OTA-089 | 2026-08-01 | Make the accepted first-window WinRT DLE prime durable across a complete transfer without pinning the link to the preset's 15 ms interval. Exact formal Type/firmware reruns reproduced 25.0-26.1 KiB/s even with `active_link_confirmed=true`; Type spent 61-64 seconds waiting at response-bearing control boundaries while local WWR submission took only 1.3 seconds. The synchronized trace showed Windows downgrade DLE from 251/965 to 251/340 about 11 seconds after the first-window request closed, after which firmware-only DLE requests received no completion. Keep the first 64-chunk proof and 400-chunk full windows. After every three successfully confirmed full windows, create a fresh request on the same secure target, hold it for the existing 500 ms minimum, close it before the next window so firmware owns 7.5 ms again, and continue without reconnecting or changing pairing. This scheduled refresh must remain below the observed downgrade horizon and keep the complete strict protocol above 60 KiB/s; if it cannot prove that in repeated current-build and installed runs, supersede it rather than publish it. | Formal artifacts `listener-104-formal-recheck-run2.json` at 26.1 KiB/s and `listener-104-formal-nocapture-run1.json` at 25.0 KiB/s; matching Type log control boundaries at 2.8-11 seconds; serial `listener-104-formal-recheck-run2.serial.log`; focused three-full-window refresh contract, current-build A/B, then three exact installed passes plus post-OTA recording smoke. |
| LST-OTA-090 | 2026-08-01 | Supersede LST-OTA-089's periodic request recreation. The candidate created and closed a successful request after each three full windows but still measured 23.7 KiB/s; full-window confirmations remained 3.3-10.9 seconds, so recreating the request after degradation did not repair the current Windows controller session. Follow Microsoft's Windows 11 contract instead: hold one `ThroughputOptimized` request for the complete firmware update, accepting its 15 ms interval and its documented reduction in simultaneous BLE connections, then release it with the exact target. Do not fight the preset with repeated firmware 7.5 ms handoffs. Establish the stable full-lifetime baseline on formal firmware, then improve only bounded OTA transfer geometry/parallelism until the complete protocol is strictly above 60 KiB/s without offset recovery, WDT, OOM, reconnect, recording or other accepted-behavior regressions. | Failed artifact `listener-104-periodic-dle-refresh-ab-run1.json`; Microsoft Learn `BluetoothLEDevice.RequestPreferredConnectionParameters` remarks requiring `ThroughputOptimized` for the duration of a firmware update; focused full-lifetime contract, current-build geometry A/B, three installed passes and post-OTA recording smoke. |
| LST-OTA-091 | 2026-08-01 | Refine LST-OTA-090 to one request on the one GATT target that carries bulk bytes. The post-reboot full-lifetime candidate still created and released a disposable prepare-target request before creating the retained secure-target request; it completed at only 19.3 KiB/s with one recovery, while the exact formal first-window handoff A/B completed at 17.9 KiB/s. Both runs spent nearly all protocol time at response-bearing control boundaries. Discovery and the disposable pre-bulk target must not request Windows preferred parameters; the freshly reopened secure bulk target must create exactly one `ThroughputOptimized` request and retain it until target drop. This experiment must not restart the Windows controller or require an OS reboot, and it is not releasable unless the existing strict speed, recovery, fixed-time, reboot, reconnect and recording gates pass. | Artifacts `listener-104-full-lifetime-post-reboot-run1.json` and `listener-104-formal-post-reboot-ab-run1.json`; focused single-secure-target/full-lifetime source contract; current-build real OTA A/B and existing release gates. |
| LST-OTA-092 | 2026-08-01 | Use a one-link-packet payload as a bounded geometry diagnostic before adding more GATT lanes or changing transports. LST-OTA-091 proved one secure-target request and held a stable 15 ms, 2M, 251-octet/965-us link for the complete transfer, with zero recovery, yet 400 aligned 443-byte writes consistently took about 11 seconds and complete throughput was only 15.8 KiB/s. Temporarily negotiate 216 data bytes (`216 + 4 OTA + 3 ATT + 4 L2CAP = 227`) so each write fits the measured 965-us packet envelope. If the existing two characteristics let Windows schedule two independent writes per event, throughput should materially increase and justify a bounded multi-lane extension; if it remains near 16 KiB/s, do not add lanes as a cosmetic workaround and prefer the existing mandatory-power USB path for deterministic updates. Do not publish this diagnostic shape unless it independently passes all release gates. | Artifact `listener-104-single-secure-lifetime-run1.json` plus synchronized serial proof of stable 15 ms/2M/251/965; focused 216-byte geometry test; one current-build real OTA comparison. |
| LST-OTA-093 | 2026-08-01 | Supersede the BLE geometry experiments as the default update path when the release PC has a likely Listener ESP32-S3 USB serial port. The one-packet LST-OTA-092 run remained 15.4 KiB/s with 7,895 writes and one recovery, proving that extra GATT lanes would not remove the Windows ATT scheduling bottleneck. Every safe firmware update already requires external power, and the same signed release ZIP already embeds the factory bootloader, partition table and app images. Type must automatically prefer its existing built-in wired flasher when a likely ESP32 port is present, use the hardware-proven 921600 baud default, retain explicit BLE OTA fallback, preserve NVS/settings, and show recent (not average) wired write speed. This path must not restart Windows or its Bluetooth controller; one device reset after verified flashing is expected. | Failed artifact `listener-104-one-packet-geometry-run1.json`; real installed-Type `COM3 / 921600` full-package flash completing in about 4.5 seconds; post-flash `~OTA:STATUS` proving clean 1.0.4 `ota_0` boot and no active target/blocker; stable follow-up audio smoke `post-wired-flash-audio-smoke-run2.json`; focused UI/default-baud/recent-speed tests, rebuilt Type package and installed validation. |
| LST-OTA-094 | 2026-08-01 | Restore the BLE OTA complete-protocol performance that 1.0.3 and accepted early 1.0.4 runs demonstrated. Diagnose the regression on the same PC and device by reproducing the tagged 1.0.3 behavior and bisecting Type, shared platform and firmware changes; fix the actual connection/GATT scheduling lifecycle rather than masking it with a peak-speed display, smaller denominator, forced Windows/controller restart, reduced reliability window or a wired-only workaround. The explicit BLE path must sustain strictly above 60.0 KiB/s for the complete 1.61 MiB protocol with zero offset recovery in three consecutive installed-release runs, while preserving the deterministic wired default/fallback, recent-speed UI, one intentional device reboot, pairing and notify recovery, recording after OTA, memory/WDT safety, LED behavior and all accepted 1.0.4 recording/wake behavior. | Same-machine tagged-1.0.3 baseline and synchronized Type/firmware trace; regression range or causal A/B evidence; focused contracts for the root fix; current-build A/B; then three consecutive exact-root installed-Type BLE passes above 60.0 KiB/s with zero recovery, fixed control time below 7000 ms, one reboot, no WDT/OOM/assert/reconnect regression, final `TYPE:READY`, and post-OTA recording smoke. |
| LST-OTA-095 | 2026-08-02 | Refine the LST-OTA-094 release boundary: an unrelated Bluetooth mouse entering its own idle sleep is not an OTA release blocker and is excluded from the formal pass condition. Publish the current 1.0.4 candidate when a normal BLE OTA run sustains strictly above 60.0 KiB/s for the complete exact 1,688,176-byte package with exactly 3,811 writes, zero offset recovery, fixed control time below 7,000 ms, one intentional device reboot, final `TYPE:READY`, and no WDT/OOM/assert/rollback. Preserve the existing Type recent-speed UI, wired 921600 path, recording/wake/speaker-verification behavior, package identities, and repository release rules. | Owner steering on 2026-08-02; current-build installed-path artifact `lst-ota-094-settle150-ab1.result.json` at 62.8 KiB/s, 3,811 writes, zero recovery and 6,977 ms fixed time; full Type tests and frontend build; firmware OTA contracts; formal MSI/ZIP rebuild, install/identity verification, final post-OTA recording smoke and release artifacts. |
| LST-OTA-096 | 2026-08-02 | Fix the Listener 1.0.4 BLE-rename LED6 red-error regression at the disconnect/cache-cleanup state-machine root. The captured rename applied the new name and later recovered `TYPE:READY`, but firmware recorded `DIAG_GAP_RECOVERY action=15 result=1` after its single 2,000 ms disconnect wait and latched `ble_recovery_disconnect_timeout` while Type was concurrently removing the same Windows pairing. Type must let firmware finish the disconnect/local-bond transition and observe the applied-name recovery advertisement before exact-address Windows cache removal; the verified handoff address remains only a bounded fallback after that scan. Firmware must treat the first 2,000 ms expiry as an incomplete asynchronous transition: reconcile against the NimBLE controller, retry termination for the same connection, and wait one further bounded 2,000 ms event window; only a controller-confirmed connection surviving both windows is a hard BLE error. Do not suppress or clear a genuine LED6 error to pass the gate. Preserve the selected name, automatic silent PairAsync, bonded recording recovery and all 1.0.4 OTA/recording/wake behavior. A real rename round trip must return final `TYPE:READY` within 15 seconds, retain the requested final name, show no `ble_recovery_disconnect_timeout`, LED6 WARN/error-domain, OOM, WDT or reboot, and complete a post-rename recording smoke. | Failure artifacts `.cache/rename-red-led-current-status.log`, `.cache/rename-red-led-diag.log` and Type timeline; focused Type ordering test plus `tools/verify_ble_rename_recovery_static.py`; full 960-test Type suite, frontend tests/build and firmware production build; installed/flashed UI-only round trip artifact `rename-red-led-ui-roundtrip.json` restored `listenerB`, reached notify-ready in 11,188/11,750 ms, and paired silently both times; post-run serial `rename-red-led-post-roundtrip.serial.log` shows both bond deletes completed as action 15/result 0, LED `error_domain=none`, WARN RGB zero, crash count zero and retained WDT count unchanged at the pre-existing one; `rename-red-led-post-recording.json` passed and returned to notify-ready. |
| LST-OTA-097 | 2026-08-02 | Fix the Listener 1.0.4 EC11 double-click Bluetooth-recovery arbitration regression at the Type state-machine root. The owner-triggered failure proved that Type received and acknowledged the EC11 pre-authorization, firmware disconnected in about 200 ms, cleared its bond and exposed the `type_recovery` advertisement with zero bonded peers, but the background listener's generation-scoped OTA-recovery flag kept classifying the explicit hardware recovery as a transient bonded-GATT failure. An acknowledged EC11 hardware-recovery notice must supersede and permanently end any older OTA-recovery semantics in that listener generation before lost-pairing and stale-cleanup arbitration; Type must scan the matching recovery advertisement, execute exactly one bounded local PairAsync transaction and restore `TYPE:READY` without an adapter restart or an unbonded connect/encryption-failure loop. Preserve genuine OTA transient-disconnect non-destructive retry, manual Windows-unpair no-steal behavior, another-host ownership, rename recovery, recording/wake behavior and all accepted 1.0.4 throughput/release gates. The installed build must recover from a real EC11 double click within 15 seconds, remain paired locally, show no repeated unbonded connect loop, LED6 hard error, OOM, WDT or reboot, and complete a post-recovery recording smoke. | Failure Type timeline at 2026-08-02 11:44:37 UTC showing EC11 pre-authorization followed by `OTA recovery generation=9 preserving Windows pairing`; board artifact `.cache/ec11-double-click-failure.serial.log` showing `type_controlled=1`, zero bonds, recovery advertising, 107 disconnects and 78 recent encryption failures without crash/OOM; focused arbitration contract plus full 961-test Type suite, frontend tests/build and required repository gates; exact installed MSI generated-EC11 path artifact `.cache/ec11-double-click-fixed.result.json` restored notify in 5,622 ms from pre-authorization through one silent PairAsync with no adapter restart or repeated unbonded loop; `.cache/ec11-double-click-fixed-post-diag.log` proves secure `type_ready`, LED `error_domain=none`, WARN RGB zero, crash count zero and no reboot; post-recovery installed recording completed with 169,600 PCM bytes, zero missing packets, non-empty recognition and notify retained. Final human boundary is one physical EC11 double click; do not ask the owner to repeat the already-recorded failure. |
| LST-OTA-098 | 2026-08-02 | Fix the Listener 1.0.4 manual Windows Bluetooth-delete reconnect loop at the Type ownership-state root. A link-loss recheck must treat only currently `Present` native Windows Listener HID roots/keyboards as live pairing evidence; historical/non-present PnP nodes left behind after Settings removes the device must not authorize persisted GATT reopen. Once both current HID and paired-device evidence are absent, Type must stop the old background notify generation, release stale GATT ownership and enter the existing passive manual-pairing hold without automatic PairAsync. The device may show its defined non-Type-ready BLE cue while unpaired, but Type must not oscillate it between low-floor double-flash and false `TYPE:READY`. A later explicit EC11 double-click recovery advertisement remains authorized for exactly one bounded silent PairAsync and must restore steady Type-ready blue within the existing 15-second gate. Preserve another-host ownership, rename/OTA recovery, active recording/wake behavior, no adapter restart, no LED6 error, no reboot and all accepted 1.0.4 throughput/release behavior. | Installed-Type timeline at 2026-08-02 12:09:38-12:10:41 UTC showing four stale GATT reopen/`TYPE:READY` cycles after manual Windows deletion because the link-loss check accepted historical HID PnP evidence; focused source/unit contract requiring the link-loss path to query present native HID evidence, Type test suite/build, then an installed manual-delete trace with no false ready/reopen followed by one real EC11 double-click recovery trace and post-recovery recording smoke. |
| LST-OTA-099 | 2026-08-02 | Supersede LST-OTA-098's use of current `Present` HID as sufficient ownership evidence and its visible manual-pairing hold. Real Windows 11 evidence shows Settings deletion can leave the old Listener HID root/keyboard marked present while its pairing is already gone; Type must always query the current paired-device view after link loss and require two missing-pair confirmations separated by a bounded 350 ms settle before stopping stale GATT reopen. Historical or temporarily present HID nodes alone cannot authorize `TYPE:READY`. Manual Windows deletion is a quiet ownership release: Type must not send the firmware manual-pairing command, start a pairing-confirmation watcher, invoke PairAsync, or intentionally cause a Swift Pair notification or low-floor find-Type double-flash. It must stop the old notify generation and wait passively with the device-side bond untouched. Only an explicit EC11 double click may clear the device bond, expose recovery advertising and authorize exactly one silent Type PairAsync; that path must still restore secure steady-blue `TYPE:READY` within 15 seconds. Preserve rename/OTA/another-host arbitration, normal reconnect, recording/wake behavior, no LED6 error/reboot and all accepted 1.0.4 release behavior. | Failed installed-Type timeline at 2026-08-02 12:25:54-12:26:33 UTC showing two false `TYPE:READY` reopens after Settings deletion despite the present-only patch; board artifact `.cache/manual-delete-still-broken-diag.log` proving `connected=1 secure=0`, `ble=pairing` and no crash; focused two-confirmation/quiet-release contracts, full Type tests/build, exact installed MSI identity, then automated installed manual-delete evidence with no PairAsync/false ready/Swift Pair ownership action followed by generated EC11 recovery and post-recovery recording smoke. |
| LST-OTA-100 | 2026-08-02 | Close the remaining manual-delete/EC11 recovery root cause across firmware and Type. An unencrypted or unbonded GATT connection must never refresh Type-host recency, Type-ready ownership, or qualify an EC11 recovery as Type-controlled. EC11 double-click recovery after Windows has deleted pairing must therefore use the native recovery path, rotate to a fresh BLE identity after disconnect, and advertise that fresh identity. While holding stale Windows HID nodes after confirmed deletion, Type may authorize its one silent PairAsync only when the explicit recovery advertisement presents an address outside the stale HID-address baseline; the old same-address advertisement is not new physical authorization. Preserve secure bonded reconnect, Type-controlled recovery on a genuinely secure current link, quiet manual-delete ownership release, rename/OTA/another-host arbitration, recording/wake behavior, no adapter restart, no LED6 error/reboot, and all accepted 1.0.4 performance behavior. | Focused firmware contracts proving rejected insecure heartbeats cannot set host-recency and both EC11 recovery entry points require a secure connection for implicit Type ownership; focused Type test proving same-address advertisements stay passive and a fresh rotated recovery identity is authorized; production firmware/Type builds, flashed/installed identity, then the exact manual-delete -> no false ready/PairAsync -> generated EC11 -> one PairAsync -> secure steady-blue `TYPE:READY` <=15 seconds machine loop plus post-recovery recording smoke. |
| LST-OTA-101 | 2026-08-02 | Apply LST-OTA-100's explicit-EC11 recovery boundary even when Windows fully removes the old HID nodes instead of leaving them `Present`. On entering the quiet manual-delete hold, Type must retain the recovery-advertisement addresses already visible at that boundary as a baseline, then keep a bounded watcher alive for a different, freshly rotated recovery identity. A baseline/same-address advertisement remains passive; a new address created by the later EC11 double click authorizes exactly one Type PairAsync transaction. The watcher must coexist with passive manual Windows reattach and be generation-scoped so restart, input-source change, shutdown or a newer hold cancels it. | Installed 1.0.4 evidence on this PC showed `baseline_native_hid_addresses=[]` and correct quiet hold but no EC11-aware watcher. Add focused empty-HID/baseline/fresh-address and generation-cancellation contracts, rerun full Type tests/build/install, then validate the no-HID manual-delete -> quiet wait -> generated EC11 -> fresh identity -> one PairAsync -> secure `TYPE:READY` machine path. |
| LST-OTA-102 | 2026-08-02 | Supersede the automatic out-of-window security-failure repair policy. A BLE encryption failure when no explicit recovery window is open is the observable result of Windows deleting or losing its bond and is not authorization to repair. Firmware must retain its local bond and identity, terminate/quarantine the insecure connection, stop connectable advertising, render BLE disconnected/dark, and wait for explicit EC11 or Type-controlled recovery. It must not delete bonds, rotate identity, open a pairing window, emit Swift Pair advertising, or enter the low-blue recovery double-flash from that failure. Encryption failure inside an already-authorized recovery window still keeps the window available for bounded host retry. | Real post-delete trace at 2026-08-02 12:56:53-12:57:14 showed Type correctly avoided PairAsync/false ready but firmware `ENC_CHANGE status=7` called the full pairing reset, rotated from `FEA4530724FA` to `C922500ED4DA`, opened Swift Pair and held `ble_repair_ms_left`; add shared policy/static contracts and a flashed exact secure-pair -> Windows UnpairAsync -> dark/no advertising/no PairAsync gate, followed by EC11 fresh identity, one PairAsync transaction, secure `TYPE:READY`, no LED6/reboot and recording smoke. |
| LST-OTA-103 | 2026-08-02 | Extend LST-OTA-102's quiet manual-delete boundary to the earlier disconnect and stale-GATT paths. A host-deliberate unpair disconnect must immediately stop all advertising and enter the explicit-recovery hold; it must not fall back from directed to undirected advertising. If a stale Type process reconnects before the deletion is recognized and sends `TYPE:READY` or a heartbeat on an unencrypted/unbonded link while no explicit pairing window is already open, firmware must terminate that link, keep advertising off and remain dark. Such an insecure command is never authorization to open a Type-controlled pairing window. First pairing and an already-open EC11 recovery window retain their existing bounded pairing behavior. | Exact installed secure-pair -> Windows UnpairAsync trace `.cache/lst102-windows-unpair-live.log` showed reason 531 restart undirected advertising, stale GATT reconnect, rejected `TYPE:READY`, and the rejection itself opening `type_controlled=1` recovery; add focused static/runtime evidence for immediate host-deliberate hold and out-of-window insecure-command quarantine, then rerun the exact delete/quiet/EC11/secure-ready/recording loop. |
| LST-OTA-104 | 2026-08-02 | A routine Type settings refresh after manual Windows deletion must not bypass the LST-OTA-102/103 explicit-recovery hold. `DEVICE:APPLY_BLE_NAME` with no pending name change is a strict no-op: it must not clear the hold, change the BLE LED state to reconnecting, terminate a link or restart advertising. A real pending rename remains an explicit user action and keeps the accepted rename recovery flow. | LST-OTA-103 post-delete status proved radio/LED RGB dark but later reported `ble=reconnecting` with `last_reason=device_settings`; Type timeline showed its normal settings probe at that boundary. Add an early pending-name gate plus static ordering evidence, then rerun the delete quiet-state sampling and EC11 recovery loop. |
| LST-OTA-105 | 2026-08-02 | The HID-level disconnect callback must not overwrite the authoritative GAP manual-delete state. When GAP has entered the explicit-recovery hold, the later duplicate `ESP_HIDD_DISCONNECT_EVENT` must retain `STATUS_LED_BLE_DISCONNECTED`; only ordinary transient disconnects may select `STATUS_LED_BLE_RECONNECTING`. This closes the delayed low-blue double-flash that remained even after advertising was correctly suppressed. | Final LST-OTA-104 trace showed GAP logging `all advertising suppressed until explicit EC11/Type recovery` and `ble=OFF`, while a later status query still reported `ble=reconnecting`; source inspection proved `ESP_HIDD_DISCONNECT_EVENT` unconditionally assigned reconnecting after the GAP callback. Add a public read-only hold query, focused callback/static evidence, and repeat the exact long delete/status/EC11 recovery loop. |
| LST-OTA-106 | 2026-08-02 | Supersede only LST-OTA-099/102/103/104/105's dark visual policy after a confirmed Windows manual Bluetooth deletion. The device must keep advertising and automatic rebond suppressed until an explicit EC11 double click, but LED2 must immediately show the existing low-blue pairing-search double pulse instead of going dark. The EC11 action must rotate to a fresh native identity and expose a connectable name-bearing recovery advertisement without the Microsoft Swift Pair manufacturer payload, so Windows does not display an Add Device or `Blistener 无法连接` notification while Type owns exactly one silent `PairAsync`. Type's local pairing-only path must try a known/direct address before its 12-second advertisement scan, and the manual-delete recovery must restore a secure steady-blue `TYPE:READY` within 8 seconds on this machine (hard ceiling 15 seconds), with no redundant same-address retry/slow AEP path in the successful case. Preserve first-time native Swift Pair, ordinary reconnect, another-host ownership, rename/OTA throughput, recording/wake behavior, no adapter restart, no LED6 error, OOM, WDT or reboot. | Owner screenshot and installed timeline at 2026-08-02 13:52:10-13:52:28 UTC: fresh EC11 identity `E211D7E8C4E6` was detected immediately, but firmware advertised Microsoft Swift Pair while Type simultaneously issued standard/custom `PairAsync`; Windows returned `DevicePairingResultStatus(19)` twice, displayed a failed-connect notification, and recovery took about 17.5 seconds. Require focused LED/advertising/direct-address-order contracts, full Type tests and firmware build, latest MSI install and firmware flash identity, then exact Windows delete -> visible search LED with no advertising -> immediate EC11 -> no system notification -> one silent pair -> secure `TYPE:READY` timing plus post-recovery recording smoke. |
| LST-OTA-107 | 2026-08-02 | Correct only LST-OTA-106's manual-delete LED wording and mapping. A low blue floor means a real BLE link exists while Listener Type is not yet ready; it must never appear after Windows has removed the pairing. During the confirmed manual-delete hold, LED2 uses the existing `PAIRING` attention pulse whose off phase reaches full black, while advertising and automatic rebond remain suppressed until an explicit EC11 double click. The duplicate HID disconnect callback and out-of-window lost-bond/encryption path must preserve that same `PAIRING` state. Other explicit holds remain dark, ordinary transient reconnect keeps its dark-phase reconnect double pulse, and a genuinely connected HID-only link retains the accepted low-floor find-Type cue. Preserve all LST-OTA-106 pairing ownership, fresh-identity, no-Swift-Pair, timing and regression gates. | Owner correction on 2026-08-02 that low blue is connected-state semantics; focused source/static checks distinguishing manual-delete `PAIRING`, other explicit-hold `DISCONNECTED`, ordinary reconnect `RECONNECTING`, and connected-only low floor; production build, flash, then serial LED state/RGB sampling before EC11 and secure `TYPE:READY` recovery afterward. |
