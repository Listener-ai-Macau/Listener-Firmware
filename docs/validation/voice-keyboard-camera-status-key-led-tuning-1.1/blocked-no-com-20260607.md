# voice-keyboard-camera-status-key-led-tuning/1.1 blocked evidence

Generated by `oai1` on 2026-06-07.

## Completed Without Hardware Lock

- `python tools\verify_status_led_static.py`: PASS after adding the status/key one-pixel calibration command and static harness checks.
- `pwsh -NoProfile -File .\tools\build.ps1 -Target esp32s3`: PASS on retry with 20 minute shell timeout. The build produced `voice-keyboard-firmware.bin` size `0xc0a50`; smallest app partition is `0x600000`, `87%` free.
- `pwsh -NoProfile -File .\tools\ai\repo_features.ps1 -Check`: PASS.
- `python -m compileall -q tools`: PASS.
- `git diff --check`: PASS.
- Camera probe: `camera0 opened=True frame=True shape=(480, 640, 3)`.

## Hardware Gate

The required locked validation command could not be run because no ESP32 serial COM port was present.

Probe command:

```powershell
for($i=1;$i -le 6;$i++){ Write-Host "probe $i $(Get-Date -Format o)"; python -m serial.tools.list_ports -v; Start-Sleep -Seconds 10 }
```

Observed output for all six probes between `2026-06-07T23:37:13+08:00` and `2026-06-07T23:38:03+08:00`:

```text
no ports found
```

## Resume

When the ESP32 serial device is visible again, resume with the plan validation command:

```powershell
pwsh -NoProfile -File ..\ai-collaboration-workflow\scripts\aiw.ps1 with-lock -Resource COMx -Run pwsh -NoProfile -File .\tools\status_led_camera_calibration.ps1 -Port COMx -Camera auto -Zones status,key -Mode rgbw-single-led
```

The harness defaults to flashing the already-built firmware with `tools\flash.ps1 -NoBuild` inside the same short hardware lock, then captures status/key LED frames and writes `docs/validation/voice-keyboard-camera-status-key-led-tuning-1.1/manifest.json`.
