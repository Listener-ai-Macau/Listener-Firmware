# Ad-hoc LED Stack Disposition

Date: 2026-06-19
Agent: oai1
Workflow step: `voice-keyboard-firmware-full-function-test/1.13a`
Worktree: `C:\Users\Billy\Desktop\Denzic\Listener\Listener-Firmware-wt-oai1-voice-keyboard-firmware-full-function-test-1.13a`
Branch: `ai/oai1-voice-keyboard-firmware-full-function-test-1.13a`

## Included LED Work

| Source | Disposition | Evidence |
|---|---|---|
| `origin/master` through `eabcc9b` | Included by fast-forward before 1.13a LED work. | Brings the accepted base, including `83490b6 Add EC11 and edge status LED accents`, `9b52989 Fix EC11 rotation direction`, `54a837c Improve LED true-state diagnostics`, `3fa3fbf Improve status LED flicker guard and shutdown confirm`, and `eabcc9b Split device timing by power source`. |
| `adhoc/oai1/status-led-recording-effect` | Included through master ancestry; no separate cherry-pick needed. | The branch tip is an ancestor of the 1.13a branch. |
| `adhoc/oai1/led-true-state-accents` | Included through master ancestry; no separate cherry-pick needed. | The branch tip is an ancestor of the 1.13a branch. |
| `adhoc/oai1/led-flicker-guard-shutdown-confirm` commit `8ee6812` | Included as 1.13a commit `def85f4 Harden status LED tail and restore recording OK`. | Carries the REC/AI status-tail guard and restores the recording OK path. |
| `adhoc/oai1/led-flicker-guard-shutdown-confirm` commit `29403fd` | Included as 1.13a commit `c41ee3d Add flicker-safe LED zone brightness controls`. | Carries `~DEVICE:SET led_status/led_key/led_ec11/led_edge` persisted 0-100 caps, LED status brightness reporting, and human review tooling. |
| `adhoc/oai1/led-flicker-guard-shutdown-confirm` commit `d48b366` | Included as 1.13a commit `d0e3f4f Finish LED product effect pass`. | Carries the product-scene LED pass: slow low-load REC/AI, EC11/edge motion, BLE re-pair cue, EC11 input feedback, low battery/charging behavior, and 1.13a disposition scaffold. |
| `adhoc/oai1/led-flicker-guard-shutdown-confirm` commit `99fa8a1` | Included as 1.13a commit `94d9d38 Stabilize REC AI status tail for LED review`. | Carries the 1.9 root-cause fix: REC/AI overlap capped to a 14%-16% slow status-tail envelope, explicit LED5/6 zeroing, effect-only preview isolation, and the final 1.9 human PASS artifacts. |
| 2026-06-19 TailOnly rework | Included in the current 1.13a branch after human prechecks. | The first TailOnly 1.13b precheck kept software and observed `LED5=OK`/`LED6=WARN` off, but failed because REC/AI motion was too subtle. The next `16%-30%` / `1.4s` attempt was visible but read as flicker; the out-of-phase and in-phase `16%-30%` / `5s` attempts still read as flashing. A later `14%-20%` / `5s` in-phase attempt still flickered even after human observation was separated from post-observation `~LED:STATUS` evidence. The `14%-16%` / `2.2s` retest then exposed separate test isolation and power issues: low-power idle could clear `preview_effect_only`, and mutating `~LED:*` review commands did not refresh power-manager activity before hardware shutdown evaluation. After TailOnly and ComboOnly passed with no flicker but no useful visible status variation at fixed 16%, single-write `16%-20% / 6s / 4%` and `16%-24% / 5.2s / 1%` retests kept LED5/6 stable but still looked nearly static. A counterphase `16%-36% / 5.2s / 1%` retest made brightness visible but read as flicker. Status-strip-only RMT TX DMA stopped the LED5/6 follow/flicker path, but the first ComboOnly retest still read LED3/4 as nearly static, so REC+AI overlap status LED3/4 is now retuned to an in-phase `16%-36% / 6s / 1%` brightness breath while overlap status frames still use only one status-strip write instead of the general three-write tail reinforcement. Hardware status capture showed ESP-IDF's ESP32-S3 RMT TX DMA path is not usable on all four simultaneous strip channels: forcing all strips to request DMA produced only `status:dma1` and left EC11/key/edge unavailable. The backend now uses a status-strip-only DMA strategy, keeps EC11/key/edge on ordinary RMT channels, preserves one RMT memory block per strip for all four V2 LED zones, and `~LED:STATUS detail=contract` reports `rmt_tx_dma_strategy=status_strip_only`, `rmt_strip_all_available=1`, `rmt_tx_dma_all_strips=0`, and `rmt_tx_dma_actual=status:1,ec11:0,key:0,edge:0`. The branch records mutating `~LED:*` commands as USB LED activity, clamps idle underflow to zero, and holds a USB-command power-manager blocker for effect-only preview until `~LED:PREVIEW clear` or `~LED:OFF`, plus the same explicit LED5/6 zeroing, guard pixels, status-strip-last transmit order, and effect-only isolation. |
| 2026-06-19 advanced status DMA effect | Included in the current 1.13a branch after the stable status-only DMA baseline passed. | REC+AI overlap status `LED3=REC` and `LED4=AI` now use a status-strip-DMA-only dual-core eased breath instead of the stable baseline triangle: in-phase `14%-40% / 6.8s / 1%`, with `2200ms` eased rise, `500ms` high hold, `2500ms` eased fall, and `1600ms` low hold. The effect still does not borrow `LED5=OK` or `LED6=WARN`, still sends only one changed status frame per overlap tick, and still reports `rmt_tx_dma_actual=status:1,ec11:0,key:0,edge:0`. This row should be judged by the later `status-only-dma-eased-dual-core` serial and human review artifacts; the earlier `16%-36% / 6s` artifacts remain the fallback stable baseline. |
| 2026-06-20 final audio-reactive REC / da-da-da AI status effect | Included in the current 1.13a branch on top of the status-strip-only DMA baseline. | Status `LED3=REC` maps only the smoothed `rec_level` envelope to a bounded `8%-72% / 2%` target effect with attack/release limiting. Status `LED4=AI` uses the final human-passed purple `哒  哒哒` cadence: one short beat, a longer gap, a grouped double beat, then rest, with `0%-90% / 2%`, `1950ms`, and contract profile `da_long_gap_grouped_dada_rest`. The change keeps status-strip-only RMT TX DMA, one overlap status write, explicit LED5/6 zeroing, and ordinary RMT for EC11/key/edge. EC11 and edge/frame continue to use the stable low warm-gold recording flow and do not follow PCM brightness. |

## Excluded Or Preserved Work

| Source | Disposition | Reason |
|---|---|---|
| `adhoc/oai1/led-flicker-guard-shutdown-confirm` merge-only commit `52c3041` | Excluded as a separate cherry-pick. | The 1.13a branch was first fast-forwarded to `origin/master`; the merge commit carries no unique product change needed after replaying the content commits above. |
| Power-hold shutdown failsafe ad-hoc stacks | Excluded and preserved. | Out of 1.13a LED-device-settings scope; belongs to a power/shutdown workflow step. |
| Hardware-revision/current-sense ad-hoc stacks | Excluded and preserved. | Out of 1.13a LED-device-settings scope; belongs to hardware revision or current-sense integration. |
| Low-power battery sampling stacks | Excluded and preserved. | Out of 1.13a LED-device-settings scope; belongs to low-power/battery behavior work. |
| BLE recovery capsule and Listener-Type settings readback stacks | Excluded and preserved. | Cross-repo or desktop-side scope; not needed for firmware LED effect readiness. |

## Current 1.13a Scope Covered

- Bounded status `LED3=REC` audio response and status `LED4=AI` final `哒  哒哒` thinking cadence that keep `LED5=OK` and `LED6=WARN` semantically off unless a real success/error/shutdown owner is active, with status-strip RMT TX DMA enabled and queryable while EC11, key, and edge remain available on ordinary RMT channels.
- Per-zone persisted LED caps: `led_status`, `led_key`, `led_ec11`, and `led_edge`.
- `~LED:STATUS detail=contract|brightness|state|power|rgb|summary` lines needed for 1.13b review, including RGB frames for status, key, EC11, and edge zones.
- Recording/processing product effects that avoid high-speed full-zone chasing on shared status paths while preserving visible EC11 and edge/frame motion.
- BLE re-pair confirmation, key/EC11 input feedback, low battery, plugged/charging/full, OK completion, and long-press shutdown confirmation coverage.
- Human review tooling and docs explaining effect-only preview modes and the 1.9 LED5/6 flicker root cause.

## Hardware Evidence

- `docs/validation/voice-keyboard-firmware-full-function-test-1.13a/all-strip-dma-status-20260619.log`: forcing all four strips to request RMT TX DMA is not viable on this ESP-IDF/ESP32-S3 path; only `status:dma1` initialized with DMA and the other LED zones were unavailable.
- `docs/validation/voice-keyboard-firmware-full-function-test-1.13a/status-only-dma-16-36-6s-contract-20260619.log`: fallback stable baseline firmware reports `rmt_strip_all_available=1`, `rmt_tx_dma_strategy=status_strip_only`, `rmt_tx_dma_requested=status:1,ec11:0,key:0,edge:0`, `rmt_tx_dma_actual=status:1,ec11:0,key:0,edge:0`, `status_tail_overlap_effect_percent=16..36_1pct_slow`, and `status_tail_overlap_period_ms=6000`.
- `C:\Users\Billy\Desktop\Denzic\ai-collaboration-workflow\docs\human-review\voice-keyboard-firmware-full-function-test-1.13b-preaccept-20260619-combo-status-only-dma-16-36-6s\human-effect-summary.md`: ComboOnly human review passed with `LED5/6 follow reports: 0`, `Irregular flicker reports: 0`, and `Brightness problem reports: 0`.
- `C:\Users\Billy\Desktop\Denzic\ai-collaboration-workflow\docs\human-review\voice-keyboard-firmware-full-function-test-1.13b-preaccept-20260619-tail-status-only-dma-16-36-6s\human-effect-summary.md`: TailOnly human review passed with `LED5/6 follow reports: 0`, `Irregular flicker reports: 0`, and `Brightness problem reports: 0`.
- `docs/validation/voice-keyboard-firmware-full-function-test-1.13a/status-only-dma-eased-dual-core-contract-20260619.log`: flashed advanced status-only DMA firmware reports `status_tail_overlap_effect_percent=14..40_1pct_eased_dual_core`, `status_tail_overlap_style=dma_eased_dual_core`, `status_tail_overlap_period_ms=6800`, `status_tail_overlap_rise_ms=2200`, `status_tail_overlap_high_hold_ms=500`, `status_tail_overlap_fall_ms=2500`, `status_tail_overlap_low_hold_ms=1600`, and `rmt_tx_dma_actual=status:1,ec11:0,key:0,edge:0`.
- `C:\Users\Billy\Desktop\Denzic\ai-collaboration-workflow\docs\human-review\voice-keyboard-firmware-full-function-test-1.13b-preaccept-20260619-combo-status-only-dma-eased-dual-core\human-effect-summary.md`: advanced ComboOnly human review passed with `LED5/6 follow reports: 0`, `Irregular flicker reports: 0`, and `Brightness problem reports: 0`.
- `C:\Users\Billy\Desktop\Denzic\ai-collaboration-workflow\docs\human-review\voice-keyboard-firmware-full-function-test-1.13b-preaccept-20260619-tail-status-only-dma-eased-dual-core\human-effect-summary.md`: advanced TailOnly human review passed with `LED5/6 follow reports: 0`, `Irregular flicker reports: 0`, `Brightness problem reports: 0`, and the note `现在没有闪烁了`.
- `docs/validation/voice-keyboard-firmware-full-function-test-1.13a/status-ai-da-dada-balanced-contract-20260620-153153.log`: final AI-only contract capture reports `rmt_tx_dma_actual=status:1,ec11:0,key:0,edge:0`, `status_tail_overlap_effect_percent=rec_audio_8..72_2pct_ai_think_0..90_2pct`, `processing_thinking_scan_profile=da_long_gap_grouped_dada_rest`, `processing_thinking_period_ms=1950`, `preview_effect_only=1`, and `active_flags=...AI:1...OK:0,WARN:0`.
- `docs/validation/voice-keyboard-firmware-full-function-test-1.13a/human-ai-da-dada-1950-summary.md`: final AI-only human review passed with `Pass/Fail/Skip: 1 / 0 / 0`, `LED5/6 follow reports: 0`, `Irregular flicker reports: 0`, `Brightness problem reports: 0`, and human note `过了`; the artifact also records the raw popup/session/serial directory.
- `docs/validation/voice-keyboard-firmware-full-function-test-1.13a/led-hardware-next-revision.md`: next hardware revision notes for eliminating residual EC11/key/edge flicker risk.

## Visual Model Artifact

`docs/validation/voice-keyboard-firmware-full-function-test-1.13a/led-effect-static-model.png` is a static model screenshot for the AI-verifiable 1.13a evidence audit. It shows the intended no-flicker baseline: REC/AI active at low duty, OK/WARN held black, EC11 all-present low warm-gold flow, and edge/frame all-present without refreshing an unchanged status rail. The current rework preserves the same OK/WARN-off invariant while allowing REC/AI overlap status output to change only through the status-strip DMA path: LED3 follows the smoothed bounded `rec_level` envelope and LED4 follows the bounded thinking cadence, both with single-write overlap frames. It is not a substitute for the real hardware visual review in 1.13b.

## Gate Status

The latest included firmware builds on the status-strip-only DMA fix and the advanced 14%-40% / 6.8s no-flicker baseline, then changes the product effect to audio-reactive REC plus the final human-passed AI `哒  哒哒` cadence. The final AI-only review passed on hardware with LED5/6 follow reports, irregular flicker reports, and brightness problem reports all at 0. The earlier 16%-36% / 6s firmware remains the fallback stable baseline if a later product-design pass asks to reduce the status animation.
